"""Shared evaluation scoring (P0).

One scorer module, imported by the CI gate, pytest, and any future UI scoreboard —
so the different surfaces can never disagree about what passed (the one-scorer rule,
Redesign/07 item 0.1).
"""
