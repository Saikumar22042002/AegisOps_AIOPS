# AegisOps scanner waivers - every skip is documented here AND in the
# PROGRESS.md scanner ledger. New checks fail the scan until triaged.
skip-check:
  - CKV_GCP_12  # network policy: sandbox cluster posture; enterprise hardening candidate
  - CKV_GCP_20  # master authorized networks would cut the demo's kubectl access path
  - CKV_GCP_21  # resource labels: cluster-level labels candidate; node labels already set
  - CKV_GCP_23  # alias IP / VPC-native requires ip_allocation_policy: plan-shape change for existing clusters
  - CKV_GCP_25  # private cluster would cut the demo's public endpoint access
  - CKV_GCP_61  # VPC flow logs + intranode visibility: cost posture
  - CKV_GCP_64  # private nodes: sandbox posture (public endpoint demo)
  - CKV_GCP_65  # Google Groups RBAC needs Workspace group setup outside the sandbox
  - CKV_GCP_66  # binary authorization: no image-signing pipeline exists in the sandbox
  - CKV_GCP_68  # secure boot for shielded nodes: enterprise hardening candidate
  - CKV_GCP_69  # GKE metadata server requires workload identity: plan-shape change
  - CKV_GCP_70  # release channel: pinned-version posture; owner-controlled upgrades
