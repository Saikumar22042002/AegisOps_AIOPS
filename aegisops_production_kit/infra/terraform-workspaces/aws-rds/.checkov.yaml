# AegisOps scanner waivers - every skip is documented here AND in the
# PROGRESS.md scanner ledger. New checks fail the scan until triaged.
# The log-export waivers were REMOVED when the enhancement shipped: the module ships
# engine-aware exports + a query-logging parameter group, secure-by-default at module level.
skip-check:
  - CKV2_AWS_60  # copy-tags-to-snapshot: day-2 candidate; enabling would alter existing instances' re-plans (B1)
  - CKV_AWS_118  # enhanced monitoring: cost posture for sandbox databases
  - CKV_AWS_157  # multi-AZ: cost posture for sandbox databases; opt-in candidate
  - CKV_AWS_161  # IAM auth: opt-in candidate; master credentials are AWS-managed (manage_master_user_password)
  - CKV_AWS_293  # TF-level deletion protection would fail governed destroy runs mid-apply; destroys are approval-gated by the platform instead
  - CKV_AWS_353  # performance insights: cost posture for sandbox databases
