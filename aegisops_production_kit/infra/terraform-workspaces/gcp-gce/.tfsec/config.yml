# AegisOps scanner waivers - documented in the PROGRESS.md scanner ledger.
# TOOL LIMITATION (see .checkov.yaml): the desired_status expression breaks the scanners'
# rendering of this resource's variable defaults; the secure defaults are unchanged and
# asserted by the committed terraform test.
exclude:
  - google-compute-no-project-wide-ssh-keys  # AVD-GCP-0030 | module default blocks them; rendering limitation
  - google-compute-vm-disk-encryption-customer-key  # AVD-GCP-0033 | CMEK posture unchanged from the original triage
