# STAB — Production Stabilization Matrix (live UI testing, 2026-07-13 → 2026-07-19)

> **Evidence base:** all 23 screenshots in `aegisops_production_kit/Screenshots/` read in full;
> file CREATED times cross-referenced against git commit times and the running Docker images.
> Status: **STEP-1 deliverable — no fixes applied yet.**

## 0 · Runtime timeline (what code was actually live when each screenshot was taken)

| Fact | Value |
|---|---|
| Screenshots **1–9** created | 2026-07-13 23:32 → 2026-07-14 00:04 |
| Screenshots **10–23** created | **2026-07-19 17:43 → 18:18 (the "NOW" evidence)** |
| BUGFIX-1..4 / INV-HON / tf-cache-share commits | 2026-07-14 00:31 → 01:11 |
| `aegisops-api:local` (bbfe840acfa3) built | 2026-07-14 **07:53** (CLN-1 `--no-cache`, sha256-verified then) |
| `aegisops-frontend:local` (b291e87acb0d) built | 2026-07-14 **08:01** |
| Commits NOT in any running image | f73707f (Postgres host-port), 438ca7c (purge-session) — both 2026-07-19, both irrelevant to the tested flows |

**Consequences.**
1. Screenshots **1–9** ran on the **pre-BUGFIX runtime** (source-mounted checkout as of ~07-13 23:30, i.e. *without* BUGFIX-1..4). Anything there that a later fix claims to cover is *pre-fix evidence*, not proof of regression — it must be retested on the fresh stack.
2. Screenshots **10–23** ran on images that **contain** the whole bugfix arc. Failures there are **live defects against current code** (only the two 07-19 commits are missing, and they don't touch these paths).

---

## 1 · Issue matrix

Class legend (tracker-vs-reality): **(a)** stale runtime · **(b)** coverage gap (test passed at a lower layer than the live path) · **(c)** regression (commit named) · **(d)** different scenario (becomes a new test) · **new** = never claimed done.

| ID | Issue (evidence) | Root cause | Fix | Prio | Class |
|----|------------------|-----------|-----|------|-------|
| **P0-1** | `terraform init failed … /app/.tf-plugin-cache/registry.terraform.io/hashicorp/google/5.45.2/linux_amd64/LICENSE.txt: permission denied` → **GCS create hard-fails** (#12, #13, #14); warm plans off the audited baseline: S3 Planner **2m28s** vs ~14s (#15, #16), azure.db Planner **1m30s** (#22, #23) | `api-test` runs `user: root` (`docker-compose.override.yml:73`) and mounts the **same `tfplugins` volume** (`:100`) as the non-root api (`docker-compose.yml:211`) — introduced by **1d27bd4** ("share the terraform plugin cache with api-test", 07-14 00:31). Root-owned provider files poison the shared cache; google install fails hard, other providers can't reuse the cache → cold init/downloads every plan. Phase-8 perms hook covers only the **tfstate** mount, not tfplugins **FIXED 2026-07-19, two root causes.** (1) Cache poisoning: api-test moved to its own `tfplugins-test` volume (root and non-root never share a cache; compose-invariant test would now catch any recurrence) + one-time volume repair + `init` retries once WITHOUT the cache on the exact poisoned-cache signature (never hard-fails a run on a cache problem). (2) Residual latency found during verification: module `.terraform` held a **675MB provider copy on the 9p/OneDrive bind mount** — `TF_DATA_ROOT` relocates it to the native tfstate volume (providers symlink from cache, execute off ext4). **Evidence (live, fresh stack):** plan stage S3 9.0s / EC2 12.6s / GCS 3.2s (baselines 14s/21s/hard-fail); UI path: GCS planner 3.8s → approval card (screenshot-13 path closed), S3 planner 18.8s (was 2m28s), EC2 planner 19.1s (≤21s baseline). S3's UI planner span still carries ~5s of show-plan/workspace bookkeeping above the 14s stage-level baseline — **owner-accepted as known 2026-07-19, no further work**. 11 regression tests | **P0** | **(c)** 1d27bd4 + **(b)** hook gap — **CLOSED** (UI-path evidence) |
| **P0-2** | DEP VPC ask re-asked identically after "use accept-ec2-net" and again after «use this accept-ec2-net" (vpc-0f411efc6ab891632)» (#1, #2) | **Timeline caveat:** #1–#2 are 07-13 23:32 — *before* BUGFIX-2 (00:41) existed. Current code maps the reply (`dependency.py:choice_from_reply` — lowercased `name in msg` matches all screenshot phrasings) and cloudops wires it (`cloudops.py:621–636`). Owner reports recurrence **NOW** (session not in the screenshot set) — suspect the router-continuation path or a pending-params record clearing `dep_ask` before the mapping runs | Retested 2026-07-19 on the fresh baked stack (api 5aa7d0915fd4, live LLM): **all three exact phrasings converge in the real UI two-turn flow** — "use accept-ec2-net" → «Placement answered · vpc → accept-ec2-net» → subnet filled from the candidate's recorded outputs → real plan; «use this accept-ec2-net" (vpc-0f411efc6ab891632)» → same; "new" → one-approval 2-step DAG (aws.vpc → aws.ec2). Playwright `stab-p02-retest.spec.ts` 3/3, single attempts. Regression spec kept | **P0** | **(a) CLOSED** — screenshots predate BUGFIX-2; fix verified on the live UI path. **Pending owner re-verify** |
| **P0-3** | After **Approve & apply**: minutes of zero feedback, reads as hung (owner live testing; no screenshot in set) | `store.ts:350–388 approveRun` **does** attach to `/approvals/{id}` SSE and handles console/token/step/done/error, and N-01 cleared the stuck flag at `done`. Gap is mid-apply **rendering**: the interrupt/approval card state isn't flipped on click, and step/console events during a long apply may not surface in the visible message/timeline the way `sendText` runs do **FIXED 2026-07-20, four root causes found during verification.** (1) *Render gap*: approveRun never re-entered the live streaming render — now instant card flip (`decision` on the message), "Approved — applying now" strip, spinner on the newest step, per-step timeline refetch. (2) *Silent denial*: the four-eyes 403 rendered as NOTHING — screenshots 15/16 show the owner (maya) self-approving her own Production run, i.e. **the original "minutes of zero feedback" was most plausibly this 403**; denials now land the backend's reason on the message and the card returns. (3) *No card restoration*: a freshly-opened session never rebuilt a pending approval card — under four-eyes the approver is never the live-attached window, so **the shipped UI could not complete a compliant Production approval at all**; openSession now restores the card from `GET /runs/{id}`. (4) *Redis-bus continuation replay*: `/approvals` consumed the run's stream from cursor 0, replayed the plan turn, and stopped at the ORIGINAL turn's `__eos__` — apply progress and `done` never reached the browser on the multi-worker posture (raw-SSE diagnostic proof); the continuation now tails from `current_cursor()`. **Evidence**: vitest 35/35 (7 new), bus-layer regression ×3, live Playwright 3/3 — visible denial · dev.engineer-initiates/maya-approves real S3 apply with live progress → `· applied` · gated destroy streaming the same | **P0** | **(d)** vs N-01 + **(b)** + a live **(c)**-class redis-bus defect — **CLOSED** (live UI evidence) |
| **P1-1** | "create a windows vm in gcp" **silently planned Linux** — gcp.vm params card offers only debian/ubuntu, "windows" in the reply dropped, plan +3 proceeded to approval (#7, #8) | `GCPVMInputs.os` allowlist is Linux-only (`workflows.py:516`) — correct, the module IS Linux-only — but the extractor/params collector silently discards the unsupported request instead of refusing | Honest refusal from the module's real allowlist: "gcp.vm is Linux-only (debian-12, ubuntu-22.04/24.04). Windows is available on aws.ec2 / azure.vm." (COMP-c family, extended to the CREATE path). **Audit all params** for silent requested-but-unsupported substitution. **Explicit audit case (owner-ordered): the s3 name rewrite** — reply `mybucket-sai@22042792002` (#4) silently became state `res-mybucket-sai-22042792002`; an invalid bucket name is re-asked with the rule stated, never silently rewritten. Each audited param gets its own utterance test | **P1** | **(d)** — COMP-c covered OS-*change* on existing VMs, never CREATE |
| **P1-2** | (a) Inventory/read answers are one dense paragraph (#9, #17); (b) "Did any instances are running in aws ?" → **classified `sync_resources`** + hallucinated narrative "user is asking to update the platform's state/memory…" (#9); (c) unreachable/rotated-away resources listed **active** — GCP discovery failed (API not enabled on rotated sandbox project) yet gcp-vm-test still "active", "AWS: 0 running EC2" yet 4 EC2 rows "active" (#17, #9) | (a) finalize renders a flat sentence list; (b) router prompt says questions⇒read (`router.py:33–49`) and the guard downgrades the *action* (`:148–153`), but the LLM's free-form intent label (`sync_resources` — string exists nowhere in the repo) and agent narrative pass through unchecked; broad-inventory fast-path misses cloud-specific phrasings; (c) INV-HON marking is **manual CLI only** — nothing flips a row when live discovery fails | (a) markdown tables / grouped lists (name · type · cloud · region · created · id); sectioned success cards; (b) deterministic questions⇒read guard extension — a question can NEVER classify as a mutating/sync intent; honest intent logging; (c) discovery-failure ⇒ rows surface as unreachable/unverified, never "active" | **P1** | (b) is **(d)** vs BUG-04 broad-inventory guard; (a),(c) **new** |
| **P1-3** | No credential download on success cards (owner live testing) | One-time reveal exists (S1) but the success card never offers a file download | "Download key (.pem)" / "Download password" wired to the existing one-time reveal (real filename, chmod-600 hint, once-only stated). Playwright create→apply→download | **P1** | **new** |
| **P1-4** | «Create a mysql db in the aws and attach the Sai-test-v1…» → terraform plan fails: `only lowercase alphanumeric characters and hyphens allowed in "identifier"` / `first character … must be a letter` (#21) | `AWSRDSInputs.identifier` has **no validator/normalizer** (`workflows.py:138`) — extractor's raw value goes straight to Terraform (exact BUGFIX-1 class: cloudsql engine got normalized, RDS identifier didn't) | Normalize at the schema (lowercase, strip invalid, must start with a letter) or honest re-ask with examples; never let TF be the validator. Utterance test with the exact screenshot ask | **P1** | **(d)** — BUGFIX-1 family, different field |
| **P1-5** | Same turn (#21): "…and **attach** the Sai-test-v1 which we created earlier" — the attach half of the request was silently dropped | COMP attach/mount honesty covers object-storage→VM mounts only; DB→instance "attach" (SG wiring / connection string handoff) is unhandled | Honest statement of what "attach" means here (SG rule + connection string to that instance; in-guest config out of scope), same family as COMP-b; never silently ignore half a request | **P1** | **(d)** — new COMP scenario |
| **P1-6** | Composer silently discards typed text: Enter during a streaming turn is a no-op — found during the P0-2 retest harness work, owner-approved as a pack item 2026-07-19 | `store.ts:246` — `sendText` early-returns while `streaming`; no queue, no disabled state, no feedback; the text stays in the box and a fast human reply loses the turn | While a turn streams: queue the message for send-on-done OR visibly disable the composer (send affordance greyed, state shown) — never a silent discard. Vitest on the store behavior + Playwright fast-reply two-turn test | **P1** | **new** |
| **P2-1** | Traces deep-link opens `langfuse:3000/...` → `DNS_PROBE_FINISHED_NXDOMAIN` (#3) | Link built from the in-network `LANGFUSE_HOST`; no browser-facing origin setting. `KEYCLOAK_PUBLIC_URL` precedent exists (`docker-compose.yml:200`, PROGRESS.md:581) | `LANGFUSE_PUBLIC_URL` rewrite for every browser-facing trace link, same pattern + same test shape as `test_auth_sso.py` | **P2** | **(b)** — HON/O1 tests asserted link *presence*, not browser-reachable origin |
| **P2-2** | "Queried AZURE · us-east-1" (#5), "Queried GCP · us-east-1" (#7) — wrong per-cloud region labels | `cloudops.py:574` interpolates the context `region` (AWS-style default) regardless of cloud; Azure uses `location` (default eastus), GCP `us-central1` | Show the real per-cloud region/location everywhere (availability step, cards, inventory rows) | **P2** | **new** |
| **P2-3** | Continuation turn logs `intent classified: None (None)` / `routed -> None agent` (#5) | Continuation path skips classification but the log line fires anyway with None placeholders | Log continuations honestly ("continuation of run … — classification skipped") or suppress | **P2** | **new** |
| **P2-4** | "Create a EkS cluster in the aws" → params card **demands raw `vpc-…` id + subnet ids** (#18), and re-demands them after "use existing vpc" (#19) | Params collection (required-field ask) runs **before** DEP resolution — `AWSEKSInputs.vpc_id`/`subnet_ids` are required with no default (`workflows.py:116–117`), so the collector blocks at raw ids; `resolve_closure` (`cloudops.py:626`) is never reached. The DEP (c) test exercised `resolve_closure` directly — a lower layer than the live path | Slot-covered params defer to DEP closure: skip DEP-fillable fields at params collection, let DEP offer world-model VPCs like EC2 does. Test at the live-path layer (params→DEP ordering), not just the resolver | **P2** | **(b)** — DEP item (c) "EKS offered existing VPCs" passed at resolver layer only |

**Also observed, no action proposed:** (i) "AZURE: live storage listing isn't wired for Azure yet" (#17) is at least honest — folded into P1-2a formatting only; (ii) sidebar Projects count 322 (07-13) vs 1 (07-19) — consistent with the owner-run purge-session (438ca7c) + fixture reseed, not classified as a defect; (iii) Incidents badge climbing 29→37 during testing — expected from the failing runs themselves; (iv) **found during the P0-2 retest**: the composer's Enter is a silent no-op while the previous turn streams — promoted to pack item **P1-6** (owner-approved).

---

## 2 · TRACKER vs REALITY

| Tracker item (marked done) | Live reality | Class | Closing evidence required |
|---|---|---|---|
| **LAT** warm-plan latency ("cold 3.97s → warm 0.002s") | S3 Planner 2m28s, azure 1m30s, GCS hard-fail **on 07-19** | **(c)** regression by 1d27bd4 (cache poisoning) **+ a latent second cause LAT never saw**: provider binaries executing off the 9p/OneDrive bind mount (module-dir `.terraform` copies) | **DELIVERED 2026-07-19** with P0-1: plan stage S3 9.0s / EC2 12.6s / GCS 3.2s; UI planner steps 18.8s / 19.1s / 3.8s (was 2m28s / — / hard fail) |
| **BUGFIX-2** DEP convergence ("LIVE-VERIFIED creds-6") | Re-ask seen in #1–#2 (pre-fix runtime); tracker claim holds on the fresh stack | **(a) CLOSED 2026-07-19** — `stab-p02-retest.spec.ts` 3/3 exact phrasings converge through the real UI; pending owner re-verify | Delivered: Playwright two-turn spec (kept as regression), transcripts showing «Placement answered» + downstream plan/DAG |
| **N-01** approval verification hang ("fixed") | Minutes of zero feedback after Approve & apply | **(d)** — N-01 fixed the terminal flag; mid-apply progress UX never covered | Playwright approve→live-progress→done on the real stack |
| **HON/O1** Traces deep-link ("done") | Link unusable from a browser (#3) | **(b)** — tests asserted presence, not reachable origin | Browser-openable link on the fresh stack |
| **COMP-c** honest OS handling ("done") | CREATE with unsupported OS silently substituted (#7–#8) | **(d)** — only the modify/OS-change path was covered | Utterance test + honest refusal in the UI |
| **BUG-04** questions⇒read guard ("done") | Cloud-specific question → `sync_resources` + hallucinated narrative (#9) | **(d)** — broad-inventory phrasings only | Router guard test with the exact screenshot phrasing; honest UI answer |
| **DEP (c)** EKS offered existing VPCs ("done") | Live params card demands raw ids (#18–#19) | **(b)** — resolver-layer test; live path asks params first | Live-path ordering test + UI run offering world-model VPCs |
| **INV-HON** inventory honesty ("done, applied live") | Discovery-failed/rotated rows still render "active" (#17) | **(d)** — INV-HON is manual-CLI by design; automatic unreachable surfacing was never in scope | Discovery-failure run showing honest status in the UI |

**Standing rule adopted (STEP-2 onward):** every commit's definition of done includes image rebuild `--no-cache` + container recreate + the CLN-1 sha256 freshness check; a STAB item seen failing in the UI closes only on evidence from that same UI path.

---

## 3 · Execution order (after STEP-2 clean runtime)

P0-1 → ~~P0-2~~ (closed (a), pending owner re-verify) → P0-3 (STOP w/ evidence after each P0) → P1-1 → P1-2 → P1-3 → P1-4 → P1-5 → P1-6 → P2-1 → P2-2 → P2-3 → P2-4 → CLN-2 sweep.

**Wave status 2026-07-20:** P0-1/P0-2/P0-3 CLOSED (live evidence). P1-1, P1-2(a/b/c), P1-3 (chmod hint — download existed), P1-4, P1-5, P1-6, P2-1, P2-2, P2-3, P2-4 **implemented + unit/regression-tested — pending the live UI utterance batch** (per the owner's per-cred-window batching directive). Live batch checklist: P1-1 windows-on-gcp refusal · P1-2 the exact sync_resources question + table render + unverified rows · P1-3 create→apply→download (sandbox-permitting) · P1-4/P1-5 the mysql-attach turn · P1-6 fast-reply queue chip · P2-1 traces link opens · P2-2 azure/gcp timeline labels · P2-3 continuation Logs tab · P2-4 EKS offers world-model VPCs (INV-HON undo staging). Creds-free work sequenced first inside each item; live cred windows spent only on live verification. Matrix items that vanish on the fresh stack close as **(a)** with image-age evidence — not re-fixed.
