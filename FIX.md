# AegisOps — Remediation & Re-Architecture Plan (FIX.md)

> **Status: BLUEPRINT. No code changes until this is approved.** This plan is the execution contract for later staged passes. Every item is grounded in [`ANALYSIS.md`](ANALYSIS.md) and the real code, cited as `path/file.py:symbol` + the `ANALYSIS §` / `Pnn` finding it addresses.
>
> Companion to [`ANALYSIS.md`](ANALYSIS.md) (the ground-truth read of the codebase). Where the two disagree, the code wins.
>
> Plan date: 2026-07-07. Branch: `feature/cloudops-v1`.
>
> **Amended 2026-07-11 (Stage A):** folded in [`AEGISOPS_TARGET_ARCHITECTURE.md`](AEGISOPS_TARGET_ARCHITECTURE.md) (the owner's consolidated target architecture). **Authority order: where that document and this plan disagree, `AEGISOPS_TARGET_ARCHITECTURE.md` wins.** Decisions 7–13 below are owner-signed and final. The [execution checklist](#8--execution-plan-checklist-stage-a-single-progress-tracker) at the end of this file is the single progress tracker for implementation.

## Decisions locked (from the steering checkpoint)

| # | Decision | Locked answer |
|---|----------|---------------|
| 1 | Harness: fix vs replace | **Fix LangGraph** — keep it as the durable checkpoint/state-machine core; re-architect the transport, drive, and add a bounded planner. Temporal deferred to a Phase‑3 decision. |
| 2 | Credential-reveal posture (P1) | **Initiator-or-approver + org-scope + always-on audit log + mandatory step-up re-auth.** Re-auth and audit are **not optional**. |
| 3 | Multi-tenancy (P2) | **Real per-user→org scoping in Phase 1** (foundation for P1/P3). |
| 4 | Multi-step planner | **Competitive phase**, bounded + approval-gated. |
| 5 | LLM provider abstraction (P10) | **`LLMProvider` interface now**, UI trimmed to what's real, extra providers later. |
| 6 | Neo4j | **Keep for now**, flag as a Phase‑3 invest-or-fold decision. *(Superseded by decision 10: resolved = INVEST.)* |
| 7 | Architecture philosophy | **Split-Trust**: deterministic Governed Core for all mutation (zero LLM trust) + Intelligent Shell (LLM planning/investigation, full autonomy read-only). Boundary = one mutating tool `execute_governed_step(cloud, resource, action, params)` whose interior is the existing pipeline: approved module → validate → plan → plan_guard → durable approval → apply (isolated remote-locked state) → verify → record. |
| 8 | U6 upgraded | The bounded planner becomes the **Governed Executive Loop**: LLM loop at the *planning* level on LangGraph/`create_agent` primitives. It drafts a **goal DAG** (each node = approved module + params, or read-only verification), gets **ONE approval for the whole DAG**, deterministic code executes steps via `execute_governed_step`, structured observations feed back, replans **deviating from the approved DAG trigger a fresh approval interrupt**. Hard bounds: max steps, max replans per step, budget ceiling. |
| 9 | Memory = Context Engine | M1–M4 implemented as five layers: **retrieval** (pgvector message embeddings + deterministic `get_turn` positional recall), **compression** (rolling LLM summary + context offloading — plans/logs stored as references, fetched on demand), **persistent memory** (per-user/org standing context under S0), **routing** (`build_context(session, budget, purpose)` into EVERY LLM call — router, cloudops, devops, sre, loop), **verification** (store-grounded answers; reconciliation verifies memory against actual cloud state). |
| 10 | D3 resolved = **INVEST** | Neo4j becomes the **live World Model + Reconciliation Engine**: cloud inventory, TF state refs, dependency edges, run/session provenance; continuous drift detection (all clouds, extend beyond AWS EC2), orphan sweep (closes P14 spend leak), impact analysis gating destroys ("what depends on this?"). Honest exit gate: if graph queries stay 1–2 hops after a quarter of real use, fold to Postgres and drop Neo4j. |
| 11 | Module Promotion Pipeline (new, Phase 3) | When no approved module exists, the agent may DRAFT one: generate → `terraform fmt`/`validate` → Checkov/tfsec scan → open as a PR-style **proposal for platform-engineer review**. Only after human promotion does it join the approved library. **Generation and execution NEVER happen in the same turn.** |
| 12 | Rejected on record | Ephemeral agent-per-tool swarms; trust-the-LLM mutation loops; runtime LLM-generated HCL execution; SDK/imperative mutation tiers ("emergency" paths); replacing LangGraph with Temporal now. Do not implement these even if a later prompt ambiguously suggests them — flag the conflict instead. |
| 13 | Decision gates | **Temporal**: trigger = long-running (hours+) / high-fan-out DAG workflows; revisit at Phase-3 exit with real data. **deepagents package**: read-only investigation agents only; re-evaluate at 1.0/LTS. Sub-agent spawning allowed for read-only work only; mutation is never delegated to a spawned agent. |

## The harness verdict (one line)

**Keep LangGraph; fix the three layers around it that are actually broken** — replace the in-process SSE bus with a Redis Streams event log, replace the fire-and-forget drive with a supervised runner + a stranded-run reconciler, and add an approval-gated multi-step planner sub-graph. Full reasoning + candidate comparison in [`docs/fix/01_harness.md`](docs/fix/01_harness.md).

## Document set

| File | Section | Contents |
|------|---------|----------|
| *(this file)* | §1 | Overview, locked decisions, **consolidated ranked problem inventory** |
| [`docs/fix/01_harness.md`](docs/fix/01_harness.md) | §2 | The harness — current assessment, proposed design, candidate comparison, end-to-end, competitive edge *(the heart)* |
| [`docs/fix/02_fixes_safety_reliability.md`](docs/fix/02_fixes_safety_reliability.md) | §3a | Safety & state isolation · Reliability · Connectivity & usability |
| [`docs/fix/03_fixes_memory_security_obs.md`](docs/fix/03_fixes_memory_security_obs.md) | §3b | Memory & continuity · Security & RBAC · Observability |
| [`docs/fix/04_fixes_ux_data.md`](docs/fix/04_fixes_ux_data.md) | §3c | UX / rendering / seamless feel · Data layer |
| [`docs/fix/05_target_architecture.md`](docs/fix/05_target_architecture.md) | §4 | The to-be architecture + a representative "create an EC2" walkthrough |
| [`docs/fix/06_seamless_parity.md`](docs/fix/06_seamless_parity.md) | §5 | Seamless-workflow parity plan + verdict |
| [`docs/fix/07_roadmap.md`](docs/fix/07_roadmap.md) | §6 | Phased execution roadmap with acceptance tests + decision gates |
| [`docs/fix/08_risks_open_questions.md`](docs/fix/08_risks_open_questions.md) | §7 | Risks, trade-offs, open questions |

Sizing legend: **S** = <1 day, 1 file · **M** = 2–4 days, several files · **L** = ≥1 week, structural. Blast radius: **low / medium / high** = how many subsystems the change disturbs.

---

## §1 — Consolidated problem inventory (ranked)

One ranked list of every serious problem from `ANALYSIS.md` (P-numbers are stable across both documents). "Current → Required" states the behavior gap the fix must close.

### Tier 0 — Must-fix to be trustworthy (safety / security / correctness)

| ID | Severity | Problem | Root cause | Files | Current → Required |
|----|----------|---------|-----------|-------|--------------------|
| **P2** | Critical | Multi-tenancy is fictional | Every endpoint uses `repo.get_default_org()`; `user.org` ignored; `Session.user_id` never set | `db/repositories.py:get_default_org`, `api/chat.py:117`, `api/sessions.py`, `api/modules.py`, `api/knowledge.py` | One shared workspace for all users → each request scoped to the authenticated user's org; sessions owned by their user |
| **P3** | Critical | No authz on session/run read + stream | Endpoints check auth only, not ownership | `api/sessions.py:59`, `api/chat.py:228`, `api/artifacts.py:_load`, `api/chat.py:218` | Any user reads any run/session by UUID → owner/org predicate; 404 on mismatch |
| **P1** | Critical | Credential-reveal not RBAC'd / no re-auth / no audit | `Depends(get_current_user)` only; no ownership, re-auth, or audit | `api/artifacts.py:209 reveal_credential` | Any authed user exfiltrates any run's private key → initiator-or-approver + org-scope + **mandatory step-up re-auth** + **always-on audit log** |
| **P5** | High | Idempotency guard double-applies on in-flight claim | Falls through to `apply()` when claim in-progress but no stored result | `agents/cloudops.py:935`, `security/idempotency.py:35` | Concurrent `/approvals` → double apply → wait-or-abort; never fall through |
| **P6** | High | Blocking `boto3` on the event loop | Sync client + `describe_instances` in an async fn | `agents/inventory.py:229 reconcile` | Every EC2 read stalls the worker → thread-offload all SDK calls |
| **P14** | Medium | Cross-store writes not atomic; no reconciliation | Apply→inventory→graph→trace are separate best-effort writes | `agents/cloudops.py:cloudops_execute`, `inventory.record_from_apply` | Crash between apply and inventory → orphaned cloud spend → same-txn inventory write + periodic reconciler |
| **P12** | Medium | Local TF backend, no state locking | `backend "local" {}`; legacy resources share `aegisops.tfplan` | all `infra/terraform-workspaces/*/main.tf`, `tools/terraform.py:61` | Concurrent ops corrupt state → remote backend + lock; unique plan-file per resource |
| **P15** | Medium | No 4-eyes; initiator not recorded | Run has no initiator; approver≠initiator not enforced | `api/chat.py:chat`, `agents/approval.py` | Self-approval possible → record `run.initiated_by`; optional approver≠initiator gate for prod |

### Tier 0 — Reliability & continuity (trust)

| ID | Severity | Problem | Root cause | Files | Current → Required |
|----|----------|---------|-----------|-------|--------------------|
| **P4** | High | Streaming single-process; channels leak | Module-global `_channels` dict; `drop_channel` only called in a test | `agents/events.py:41`, `api/chat.py:_drive` | Memory leak + multi-worker broken → Redis Streams event bus; evict on terminal |
| **—** | High | Crash mid-run strands the run | `_drive` is fire-and-forget `asyncio.create_task`; only the approval pause is resumed, not a crash mid-apply | `api/chat.py:171`, `agents/runner.py` | A run in `running`/`awaiting_approval` after a crash never re-drives → supervised runner + stranded-run reconciler |
| **Memory** | High | Conversational recall is lossy | Char-budgeted transcript; older turns → 160-char digest; router sees only 8 turns | `agents/memory.py:build_transcript`, `classification_context` | "100th message recalls the 20th" only partially → deterministic positional + semantic per-session retrieval; token budget + rolling summary |

### Tier 1 — Needed to be competitive (governance honesty, UX, autonomy, observability depth)

| ID | Severity | Problem | Root cause | Files | Current → Required |
|----|----------|---------|-----------|-------|--------------------|
| **P7** | High | SRE remediation is a no-op reporting success | Only lists deployments; hardcoded `recent_deploy:True` | `agents/sre.py:146`, `agents/sre.py:53` | Approves a remediation that does nothing → real K8s actions + real Prometheus signals, or explicit "proposed, not executed" |
| **P8** | High | Policy checks are hardcoded `True` | `_ck(name, True)` regardless of inputs/plan | `agents/templates.py:_s3_policy…_gcp_cloudsql_policy` | Approver trusts theater → evaluate against `validated`/plan JSON; OPA/Conftest option |
| **P9** | Medium | Traces tab shows static placeholders | Returns fixed span names, ignores Langfuse | `api/artifacts.py:184 traces` | Real trace tree not shown → query Langfuse or deep-link |
| **P10** | Medium | Model selection is fake | `body.model` never read; global Gemini singleton | `api/chat.py`, `integrations/gemini.py:get_gemini`, `frontend/lib/data.ts:42` | UI lies about model choice → `LLMProvider` interface; honor `body.model`; trim UI |
| **P11** | Medium | "Ask which cloud" unreachable from UI | Selector defaults `AWS` | `agents/cloudops.py:resolve_cloud`, `api/chat.py:40`, `frontend/lib/store.ts:137` | Never asks in real UI → "Auto/ask me" default; selector as hint only |
| **P16** | Low/Med | DevOps CI verify doesn't wait | Reads `latest_run_status` immediately after dispatch | `agents/devops.py:170` | May read the previous run → capture run id + poll to completion |
| **P13** | Medium | Dead code: `runinput:`, `drop_channel`, `route_decision`, `prior_user_questions` | Wired but unconsumed | `api/chat.py:248`, `agents/events.py:54`, `agents/router.py:195`, `agents/memory.py:53` | Interactive input is a no-op → wire mid-run input to `console.send_input`; remove/wire the rest |

### Tier 1 — Connectivity & usability (already mostly-fixed; verify + harden)

| ID | Severity | Problem | Root cause | Files | Current → Required |
|----|----------|---------|-----------|-------|--------------------|
| N-02 | (verify) | VM reachability / credential delivery | `allowed_cidr` + one-time reveal exist | `infra/terraform-workspaces/*-vm|gce/main.tf`, `agents/cards.py`, `api/artifacts.py` | Real, but reveal is under-secured (P1) → keep + secure |

### Tier 2 — Hardening / differentiators

| ID | Severity | Problem | Files | Current → Required |
|----|----------|---------|-------|--------------------|
| **P17** | Low | Notify emails sender, not stakeholders | `agents/notify.py:46` | Self-email → address initiator/approver/team |
| **P18** | Low | Gemini sync network call in constructor | `integrations/gemini.py:_resolve` | Blocks first call → lazy/async resolve |
| **P19** | Low | Rate-limit on SSE; unused metrics | `main.py`, `metrics.py` | Empty dashboard series → exempt SSE; wire/remove metrics |
| **P20** | Low | `answer`/`outcome` not redaction-scanned pre-persist | `api/chat.py:_persist_result` | Future secret echo persists → `redact()` backstop |
| Data | Med | Missing hot-path indexes | `db/models.py` | Seq scans → add `messages(session_id,created_at)`, `messages(run_id)`, `runs(session_id)`, `runs(org_id,created_at)` |
| Neo4j | — | Best-effort mirror earns little | `graph_db/*` | Invest or fold — Phase‑3 decision |

**Coverage note.** Every P-item and every gap in `ANALYSIS.md` §09/§10 is carried into a domain fix ([02](docs/fix/02_fixes_safety_reliability.md)–[04](docs/fix/04_fixes_ux_data.md)) and placed in a phase ([07](docs/fix/07_roadmap.md)). Nothing from the analysis is dropped.

---

## §8 — Execution plan checklist (Stage A, single progress tracker)

> Appended at Stage A (2026-07-11). **This checklist is the single progress tracker** for Stage B; status is updated per item as implemented and mirrored into `aegisops_production_kit/PROGRESS.md`. Full item specs + acceptance tests: [07 roadmap](docs/fix/07_roadmap.md) and the domain fix docs. Work strictly in phase order; a phase's exit gate must pass before the next phase starts.
>
> **UI surface legend (§5 of the production directive):** ① tenancy visible · ② roles honest · ③ reveal flow · ④ cloud selector · ⑤ model menu · ⑥ approval card · ⑦ traces tab · ⑧ SRE honesty · ⑨ memory in the UI · ⑩ executive-loop UI · ⑪ drift & orphans · ⑫ streaming. Status: `pending` → `in progress` → `done (evidence)`.

### Phase 1 — Trustworthy

| ID | Item | Files touched | Acceptance test | UI | Status |
|----|------|--------------|-----------------|----|--------|
| S0 | Real multi-tenancy: principal→(org_id,user_id); kill `get_default_org()` everywhere; `Session.user_id`; org predicates; seed **two orgs with users** | `security/tenancy.py` (new), `security/deps.py`, `schemas/auth.py`, `db/repositories.py` (`org_for`, `get_default_org` deleted), `api/{auth,chat,sessions,modules,knowledge}.py`, `seed/seed.py`, `infra/keycloak/realm-export.json` (org groups + membership mapper + org-B users), `rag/embeddings.py` (keyword degrade) | `test_tenancy.py`: two-org isolation; cross-org UUID → 404; visible in UI | ① | **done** — `tests/test_tenancy.py` 8/8 (resolver matrix: claim wins/mirror update/seeded fallback; sessions+chat+approvals+overview+knowledge isolation, cross-org 404; strict 403 for unscoped principal); full suite 402 passed/2 skipped; vitest 25 passed. Orgs seeded: northwind-financial + acme-industrial. Flag `AEGISOPS_TENANCY=strict\|legacy` (default strict). Keycloak org claim via group-membership mapper; existing dev realms need a keycloak container recreate to pick it up (username/email fallback covers until then) |
| S1 | Credential reveal: initiator-or-approver + org-scope + step-up re-auth (≤120s proof) + always-on audit; frontend re-auth modal | `api/artifacts.py:reveal_credential`, `security/deps.py:verify_stepup_auth`, `settings.py` (`REVEAL_STEPUP_MAX_AGE_SECONDS=120`), `frontend/…CredentialReveal` (password modal) | non-owner → 404; no fresh proof → 401; value once, 2nd → 410; every attempt audited | ②③ | **done** — `test_tenancy.py::TestCredentialRevealS1` (cross-org→404, non-initiator/approver→404, initiator w/o proof→401, bad password→401, approver+fresh proof→value once, 2nd→410; audit-row count == attempts, value never logged); reveal modal collects password re-entry, surfaces 401 in place; full suite 407 passed/2 skipped; vitest 25; tsc clean |
| S2 | `authorize_run`/`authorize_session` on every read/stream; 404 on mismatch | `security/deps.py` (shared predicates), `api/artifacts.py:_load` (all 8 tabs + credentials + metrics), `api/chat.py:get_run`/`chat_stream`, `api/sessions.py:session_messages` | cross-org read of every tab + stream → 404 | ① | **done** — `test_tenancy.py::test_cross_org_read_of_every_tab_is_404` (9 run reads + stream + messages + credentials cross-org → 404; same-org → 200; invalid UUID → 404 not 500); full suite 403 passed/2 skipped; vitest 25 passed |
| S3 | `require_initiator` on `POST /chat` | `api/chat.py`, `frontend/components/Workspace.tsx` (read-only composer notice) | read-only POST /chat → 403; composer honest for read-only | ② | **done** — `test_rbac_endpoints.py::test_chat_requires_initiator` (read-only/auditor → 403 at the gate; all 5 initiator roles pass it); composer replaced with a clear read-only notice for `can_initiate=false`; full suite 404 passed/2 skipped; vitest 25 passed; tsc clean |
| S4 | Redaction backstop on persisted `answer`/`outcome` | `api/chat.py:_persist_result` | planted secret masked in persisted content | — | **done** — `test_redaction.py::TestPersistBackstop` (planted private key + password masked in `messages.content` and `runs.outcome`; non-secret fields preserved); full suite 405 passed/2 skipped |
| S5 | Capability assertion at the execute node | `agents/execute.py` (fail-closed), `agents/approval.py` + `api/chat.py` (carry approver `can_execute` through the resume) | execute refuses when the approver lacks execute capability | — | **done** — `test_safety_invariants.py::TestExecuteCapabilityGuard` (3). Deviation from fix-doc "initiator": asserts the **approver's** capability — an initiator (developer) legitimately lacks execute, so asserting the initiator would wrongly block every developer-initiated run; the approver authorizes mutation. Full suite 424 passed/2 skipped |
| A1+B7 | Idempotency wait-or-abort + `/approvals` endpoint guard + lifecycle primitive | `security/idempotency.py` (`is_in_progress`, `wait_for_result`), `agents/cloudops.py:cloudops_execute` (wait-or-abort, never fall through), `api/chat.py:resolve_approval` (NX in-flight lock) | concurrent double-approve → exactly one apply; in-progress never falls through | — | **done** — `test_idempotency.py` (+4: in-progress helper, wait returns stored/None/abort-at-deadline, **node aborts on in-flight claim with apply() asserted unreachable**), `test_tenancy.py::test_double_approval_endpoint_guard` (2nd /approvals → 409); full suite 412 passed/2 skipped |
| A2 | plan_guard re-asserted at the approval node | `agents/approval.py` (choke-point re-assertion before `interrupt()`) | approval refuses mismatched plan even if plan node skipped the guard | ⑥ | **done** — `test_safety_invariants.py::TestApprovalChokePointGuard` (apply-with-replace blocked before interrupt; destroy-that-creates blocked; explicit read-action honored; blocked routes to finalize not execute); full suite 416 passed/2 skipped |
| A4 | Org-scoped duplicate-name check | `agents/cloudops.py:314` (no logic change — `list_active(state["org_id"])` now carries the real S0 org) | dup check carries the real org | — | **done** — `test_inventory.py::test_duplicate_name_check_is_org_scoped` (a name active in org A is invisible to org B; the same-name-create dup predicate fires only within the owning org; no cross-org leak). Verified, not rebuilt: S0 flows the real org into `state["org_id"]` |
| A5 | `runs.initiated_by` + policy-configurable 4-eyes (Production) | migration `0004_run_initiated_by` (+`runs.env`), `db/models.py`, `api/chat.py`, `settings.py` (`AEGISOPS_FOUR_EYES_FOR_PRODUCTION`, default on) | prod self-approve → 403 | ② | **done** — `test_tenancy.py::test_four_eyes_blocks_prod_self_approval` (prod self-approve → 403 four-eyes; different approver passes the gate; non-prod exempt); full suite 406 passed/2 skipped |
| B5 | Terminal-state guarantee (finally + fault-injection test) | `api/chat.py` (`_force_terminal` backstop + `except` in both `_drive` closures) | fault in `_persist_result` → run `failed`, never stuck | — | **done** — `test_tenancy.py::TestTerminalStateB5` (force-terminal marks a running run failed; leaves a completed run untouched; fault-injection through the real /chat endpoint → run ends `failed`, not `running`) |
| B6 | Zero blocking I/O: `inventory.reconcile`, sync-SDK grep-audit, Gemini sync-resolve (P18) | `agents/inventory.py:reconcile` (boto3 describe → `anyio.to_thread`), `integrations/gemini.py` (lazy off-thread `_ensure_model`, no network in constructor) | responsiveness test; clean grep audit | — | **done** — `test_inventory.py::test_reconcile_offloads_blocking_sdk_call` (concurrent ticker runs ≥10× during a 0.4s blocking describe → loop never stalled); grep-audit: all remaining agent SDK calls go through the offloaded `tools/aws.py`; Gemini model resolve moved out of `__init__` to a lazy `anyio.to_thread` call (P18); full suite 418 passed/2 skipped |
| U4 | "Auto (ask me)" cloud default; selector-as-hint | `frontend/lib/data.ts` (Auto option + `cloudToWire`), `frontend/lib/store.ts` (default + wire map), `api/chat.py` (`ChatContext.cloud=None`) | ambiguous VM request → clarifying question appears in UI | ④ | **done** — `cloud_selector.test.ts` (Auto is first option, maps to null; real clouds sent verbatim) + existing `test_routing_scenarios.py::test_ambiguous_cloud_asks_never_defaults_to_aws` (null selector + generic VM → resolve_cloud None → clarifying question). `resolve_cloud` already handled null; only the AWS default needed removing. Full suite 424 passed/2 skipped; vitest 28 |
| HON | Honesty labels: policy "not evaluated"; SRE "proposed, not executed"; Traces "coming — open in Langfuse" deep-link | `agents/templates.py` (`_todo`/`evaluated` flag), `agents/sre.py` (proposed-not-executed), `api/artifacts.py` (traces deep-link + timeline count), `frontend/components/ArtifactPanel.tsx` (not-evaluated render + Langfuse link) | no surface claims what it didn't verify | ⑥⑦⑧ | **done** — P8: `test_templates.py::test_policy_checks_never_fake_a_pass` + updated `test_s3_policy_checks` (literal-True checks → `passed=None` "not evaluated", real predicates keep bool; a false predicate shows a real fail). P7: `test_honesty_labels.py::TestSreHonesty` (never `applied:True`; outcome `proposed_not_executed`; user told "proposed, not executed"). P9: `TestTracesHonesty` (no fabricated spans; `coming_soon` + Langfuse deep-link, trace_id==run_id). Full suite 427 passed/2 skipped; vitest 28 |
| O2 | Langfuse project-key startup assertion | `settings.py` (`LANGFUSE_EXPECTED_PROJECT`), `integrations/langfuse_client.py:assert_project`, `main.py` lifespan | startup asserts key/project | — | **done** — `test_langfuse_tracing.py` (+3: keys match→ok; wrong project→"wrong_project" loud warning; no keys→"not_configured"). Queries `/api/public/projects` with the keys, warns loudly if the expected project isn't among them (catches the exact "0 traces / wrong project" regression). Best-effort; never blocks startup |
| C1 | Per-cloud plan-assertion tests: ingress/CIDR | `tests/test_module_ingress.py` | SG/NSG/firewall rules asserted per cloud | — | **done** — `test_module_ingress.py` (6): admin port bound to `allowed_cidr` only (never 0.0.0.0/0) on AWS SG / Azure NSG; GCP firewalls attach via network tags (the exact regressed defect); app ports from `ingress_ports`. Static source assertion → runs anywhere; the live `terraform show -json` superset is creds-gated |
| D1 | Hot-path indexes | migration `0005_hot_path_indexes` | `EXPLAIN` shows index scans | — | **done** — `test_indexes.py` (2): the 4 indexes exist (`messages(session_id,created_at)`, `messages(run_id)`, `runs(session_id)`, `runs(org_id,created_at)`); the transcript query plans onto `ix_messages_session_created` |
| D4 | Repo/state hygiene: purge tracked `*.tfplan`, gitignore, dev TF state off OneDrive | `.gitignore` (+`*.tfplan`, `terraform.tfstate.d/`), purged 11 tracked `*.tfplan` from the index | `git status` clean; warm plan faster | — | **done** — 11 `aegisops.tfplan` files removed from git tracking (they embed variable values); `*.tfplan` + `terraform.tfstate.d/` gitignored; 0 tracked plan/state files remain. Compose already mounts the `tfstate` named volume off the bind-mount; the durable dev-state-off-OneDrive fix is A3 (remote backend, Phase 2) — noted, not silently skipped |

**Phase 1 exit gate:** two orgs isolated in API+UI · read-only can't initiate · no prod self-approve · exactly one apply under concurrent approve · reveal gated+audited · no dishonest surface · regression suite green. — **status: pending**

### Phase 2 — Production harness + Context Engine

| ID | Item | Files touched | Acceptance test | UI | Status |
|----|------|--------------|-----------------|----|--------|
| E2E | **(owner-ordered, first)** Playwright e2e for §5 flows 1–3 + re-run existing browser suite | `frontend/e2e/tenancy_roles_reveal.spec.ts` (new) | flows 1 (tenancy visible), 2 (read-only composer), 3 (reveal step-up modal) | ①②③ | **done** — `tenancy_roles_reveal.spec.ts` (6): org-A sees Northwind; org-B sees Acme only, never Northwind; read-only shows the notice + no composer; initiator sees composer; reveal → step-up modal → wrong password stays with re-auth message → correct shows the value once. **Full browser suite green: 22 passed / 2 skipped (by-design mobile) / exit 0**; 1 live-LLM streamed-run test is flaky-but-passes here (invalid Gemini key → slow backend retries; clean with a real key). Frontend served by host `next dev` (live code; the compose image was stale) |
| B1 | Redis Streams event bus; `Emitter` unchanged; `AEGISOPS_EVENT_BUS=memory\|redis` | `agents/events.py` (`RedisChannel` + XREAD pump, memory path byte-identical), `settings.py` (flag), `api/chat.py:chat_stream` (redis existence check + cursor) | SSE contract on Redis; multi-worker publish/consume; TTL on terminal | ⑫ | **done** — `test_event_bus_redis.py` (5): frame shapes identical; exactly-once + EOS stops (marker never leaks); replay-after-id; **multi-worker publish-A/consume-B**; terminal stream sets TTL. Memory path unchanged — `test_sse_contract.py` (7) still green. Default `memory` (rollback); `redis` opt-in via flag. Full suite green |
| B2 | RunSupervisor: tracked tasks + heartbeat + graceful drain | new `agents/supervisor.py`, `api/chat.py` (both drive sites → `supervisor.run`), `main.py` (drain on shutdown) | heartbeat expiry observable; honest shutdown persistence | — | **done** — `test_supervisor.py` (2): a supervised run is `is_live` with a `run:<id>:hb` key, deregisters (key cleared) on normal completion; **drain cancels an in-flight run and persists it `failed`** (never dropped). Replaces the fire-and-forget `create_task`; heartbeat TTL 45s / refresh 15s feeds B3's stale-run detection |
| B3 | Stranded-run reconciler | new `agents/reconciler.py`, `main.py` (gated start/stop in lifespan), `settings.py` (`AEGISOPS_RECONCILER=on\|off`), `docker-compose.override.yml` (off under pytest) | kill-mid-apply → terminal exactly once | — | **done** — `test_reconciler.py` (5): stranded non-resumable → failed honestly; resumable → re-driven via supervisor (not failed); live/fresh-heartbeat skipped; awaiting_approval left; **kill-mid-apply → terminal once, no re-apply**. **Defect fixed**: reconciler no longer auto-starts a background loop in TestClient lifespans (gated off under pytest) + `start()` idempotent. Full suite green **twice in a row** (450/2/exit 0 each; direct exit code, no pipe-masking). **Gate defect fixed (2026-07-12, found in the live worker-kill demo)**: `_redrive` ran the graph but never persisted the result → a successfully re-driven run stayed `running` and the next sweep force-failed it (two reconciler actions; a redriven apply's outcome would've been stamped over with `failed`). The redrive now persists status + assistant message via `_persist_result` (interrupted→awaiting_approval; B5 `_force_terminal` backstop) and emits `done`. Evidence: `test_redrive_persists_the_result_and_second_sweep_is_a_noop` + the live re-demo (exactly one reconciler action) |
| B4 | Verify cross-cloud (Azure/GCP), timeout-bounded, offloaded + honest cards (C2) | `agents/finalize.py:_reconcile_checks` (Azure `list_vms` / GCP `list_all_instances` branches, matched by VM name) | per-cloud mock-slow → warn card, never hangs | — | **done** — `test_verify_cross_cloud.py` (5): Azure/GCP VM present → real live check; missing/terminated → real **failed** check (not fake pass); Azure slow-SDK → verify warns within its bound, never hangs. Readers thread-offload; the reconcile stays 30s-bounded. C2: the success card's host/connection already derive from generic outputs (public_ip/login_user) which Azure/GCP VMs provide |
| A3 | Remote TF backend (S3+DynamoDB, `-backend-config`); unique plan-file per run; `AEGISOPS_TF_BACKEND` | `tools/terraform.py` (`run_id` → plan-file, `_backend_config_args`), `settings.py` (`AEGISOPS_TF_BACKEND`, `TF_STATE_KEY_PREFIX`), `agents/cloudops.py` (4 sites thread `run_id`) | safety-live green on remote; no plan-file collision | — | **done (local; remote pending infra)** — `test_terraform_backend.py` (6): unique plan-file per run (no collision; plan+apply reuse the same path); backend-config empty in local, correct S3+DynamoDB `-backend-config` (state key namespaced per module+workspace, DynamoDB lock) in remote, no-op without a bucket. **Remote apply is untestable here** (no S3 bucket / DynamoDB table + the module backend blocks are `local` — switching to `s3` is the documented migration); plumbing + flag land now, remote apply recorded as pending infra |
| LAT | Latency pass: init-skip + `TF_PLUGIN_CACHE_DIR` | `tools/terraform.py` (`_is_initialized` + skip-with-fallback in `init`, plugin-cache env), `settings.py` (flag + `TF_PLUGIN_CACHE_DIR`), `docker-compose.yml` (`tfplugins` volume) | warm turn → approval card ≤15s; measured in PROGRESS.md | ⑥ | **done (init measured; full-turn ≤15s pending creds)** — `test_terraform_latency.py` (5, incl. **warm-skip falls back to full init when the module claims initialized but the provider cache is gone** — the risk-doc "fall back on any mismatch" guard; found + fixed via `TestStateIsolation`). **Measured init before/after (demo-null): cold 3.97s → warm 0.002s (skipped), −3.97s/turn.** Real cloud providers are far larger (~19s cold per the prior audit) — eliminated on warm turns; the plugin cache reuses providers across modules. Full-turn ≤15s (init≈0 + cloud `plan` ~21s + LLM) needs a creds-enabled turn to confirm end-to-end — recorded as pending. The warm-skip also avoids the residual-backend re-init prompt that fails a cold re-init |
| M1 | `build_context(session, budget, purpose)` into EVERY LLM call; purpose slices | `agents/memory.py:build_context`, `agents/{router,general,knowledge}.py` | 40-turn context within budget; agents see prior turns | ⑨ | **done** — `build_context(purpose=…)` (per-purpose budget) threaded into router/general/knowledge, returning a relevant-earlier-turns slot + transcript. `test_memory.py::test_build_context_*`, `test_general_agent_prompt_includes_history` |
| M2 | Embeddings on write + semantic retrieval + deterministic `get_turn`; embedding flag | migration `0006_message_embeddings` (+HNSW), `db/models.py` (`Message.embedding`), `agents/memory.py` (`get_turn`/`detect_recall`/`retrieve`/`embed_message`), `api/chat.py` (embed-on-write), `agents/general.py` (deterministic recall) | **turn 20 of 100 verbatim** | ⑨ | **done (UI demo at the gate)** — gate finding (2026-07-12): the natural "what did I say in **turn 20**?" phrasing matched neither noun-last recall form → added the noun-first `turn N`/`message #N` shape to `_RECALL_RE` (deliberately NOT `request N`/`question N` — false-positives on "I request 3 VMs"); `test_detect_recall_parses_turn_n_shape` + guards. — `test_memory.py` (6): `get_turn(20)` verbatim; `detect_recall` parses positional queries; `build_context` includes turn-20 full text; keyword retrieval finds a turn by content; **general answers exact recall verbatim without the LLM**. Embed-on-write is best-effort (NULL without a Gemini key → pg_trgm keyword recall). UI end-to-end demo needs a valid Gemini key (router/general call the LLM) — recorded pending |
| M3 | Router purpose-slice replaces last-8-turns | `agents/router.py` (`build_context(purpose="router")`), `agents/memory.py` | 30-turn-old reference resolves | ⑨ | **done** — router now uses `build_context(purpose="router")` (summary+recent+retrieval) instead of the fixed last-8 window, so a reference to an earlier turn resolves via retrieval |
| M5 | Context offloading: plans/logs/dumps as references, fetched on demand | `agents/memory.py` (`plan_ref_line`, `fetch_plan`) | long session in budget; prior plan answered from artifact | — | **done** — plans live in `runs.plan_json` (never inlined into the transcript/prompt); `plan_ref_line` is the short reference the context carries, `fetch_plan(run_id)` fetches the full plan on demand. `test_memory.py` (2): a 30-plan session stays within the purpose budget with no raw plan JSON inlined; `fetch_plan` returns the stored 8-change plan |
| U1 | Real policy predicates over `validated` + plan JSON | `agents/templates.py` (`_after`/`_block0` + real predicates), `tools/terraform.py` (`planned_resources()` from `show_plan`, in-memory only — not persisted), `agents/cloudops.py` (2 sites pass the plan) | encryption-off plan → **failed** check on the card | ⑥ | **done** — `test_policy_real.py` (7): EC2 encryption-off / IMDSv1 → **failed**; hardened → pass; S3 public-access-open → failed; RDS unencrypted/public → failed; Azure storage weak-TLS/public → failed; no-plan → honest "not evaluated". Predicates read the real `change.after` (root_block_device.encrypted, metadata_options.http_tokens, public_access_block, storage_encrypted, min_tls_version). `test_templates.py` still green. Raw `after` used in-memory for eval only — never persisted to `runs.plan_json` |
| DEF | Defaults honesty: defaulted VPC/subnet/network/RG stated on the approval card | `agents/cloudops.py:_defaulted_dependencies` (+ `defaults` in plan_json/interrupt), `frontend/components/ArtifactPanel.tsx` ("Defaults applied" card) | "placing in default VPC vpc-0abc" visible | ⑥ | **done** — `test_defaults_honesty.py` (6): AWS EC2 default VPC surfaced (resolved subnet-id named when the plan reveals it); GCP VM default network; Azure VM auto-created `<name>-rg`; a user-specified dependency is NOT flagged. The approval card renders an amber "Defaults applied" section. vitest 28; tsc clean |
| U2 | SRE real: Prometheus deploy-annotation signal; real K8s actions or "proposed, not executed" | `agents/sre.py` (real `recent_deploy`/cpu/restart signals; execute dispatch), `tools/kubernetes.py` (`restart_deployment`/`scale_deployment`/`rollback_deployment`) | fake-K8s patch real; without K8s honest label; never fake success | ⑧ | **done** — `test_sre_remediation.py` (8): `recent_deploy` from a real Prometheus query (deployment-generation change), False without Prometheus (no longer hardcoded True); with a cluster, restart/scale(+1)/rollback issue the real AppsV1 patches → `applied:True`; a failed action → `remediation_failed`; investigate never mutates; **no cluster → still "proposed, not executed"** (honesty test green). Real K8s ops are behind the approval gate |
| U3 | LLMProvider + GeminiProvider + `get_provider(body.model)`; menu trimmed | new `integrations/llm/` (`base.LLMProvider`/`UnknownModelError`, `gemini_provider`, `registry.get_provider`/`available_models`), `integrations/gemini.py` (per-run model contextvar), `agents/llm.py`, `api/chat.py` (400 on unknown), `api/integrations.py` (`GET /models`), `frontend/lib/data.ts`+`store.ts`+`colors.ts` | model selection real; unknown model → clear error | ⑤ | **done** — `test_llm_provider.py` (9): `get_provider(None)`→default, known id→that id, unknown→`UnknownModelError` naming what we serve, catalog all-Gemini with one default & no phantom vendors, per-run model isolated across concurrent tasks (contextvar, not singleton mutation), `_effective_model` precedence explicit>run>default, `GET /models` real catalog, **`/chat` with `gpt-4o`→400 before any DB**. Menu trimmed to the 3 real Gemini ids; store default now a served id. vitest 28, tsc clean |
| O1 | Real Traces tab: run_steps-derived tree + Langfuse deep-link | `api/artifacts.py` (`_trace_spans` + `traces`), `frontend/components/ArtifactPanel.tsx` | real durations, no `—` | ⑦ | **done** — `test_traces_tree.py` (4): root run span + ordered children from real run_steps, each duration the step's actual elapsed (`480ms`/`3.2s`/`1m04s`, no fabricated `—`); an in-flight step shows `···` (and a running run's root too — never a partial total); failed steps marked red + carry the truncated error; tool/human/retry annotations. Deep-link to the full Langfuse trace shown alongside the tree; no-steps runs fall back to the honest note + link (`coming_soon` retired). Honesty test updated (`coming_soon=False`). vitest 28, tsc clean |
| O3 | Metrics hygiene; SSE exempt from rate limiter | new `ratelimit.py` (shared limiter, no import cycle), `main.py`, `metrics.py`, `agents/timing.py`, `api/chat.py` | non-empty series or removed; SSE unthrottled | — | **done** — `test_metrics_hygiene.py` (6): the 3 always-empty series fixed — `AGENT_STEP_DURATION` now observed with real per-step elapsed in `timing.end_step` (proven by a live-DB sample), `APPROVAL_WAIT` observed in `resolve_approval` with the real human wait (domain/decision; unit-proven via `_record_approval_wait`, skips when no start), `TOOL_RETRIES` **removed** (no real source in scope). SSE endpoints (`POST /chat`, `GET /chat/stream/{id}`) marked `@limiter.exempt` — verified in `_exempt_routes`, while a normal write route stays limited |
| D2 | Same-txn inventory write + orphan sweeper | `agents/cloudops.py` (atomic write), `agents/inventory.py` (`inventory_payload`/`upsert_resource`/`recover_missing`/`mark_destroyed_txn`), `agents/reconciler.py` (`sweep_orphans`) | crash-inject → no invisible orphan | — | **done** — `test_orphan_inventory.py` (4): apply (irreversible) now writes the inventory row + run outcome in ONE txn, and the outcome carries a self-contained `_inventory` recovery payload; **crash-inject (outcome=applied, no row) → `sweep_orphans` rebuilds the row from the payload — no cloud read**; sweeper idempotent (never duplicates a visible row); legacy run without payload is left alone. Cloud-level orphan where the DB txn never ran (apply done, worker died pre-persist) still needs a live TF backend to reconcile from state — recorded pending (no creds) |
| U5 | Mid-run input: wire via supervisor or remove endpoint+key (document choice) | `api/chat.py` (removed `POST /runs/{id}/input`), `tools/console.py` (removed `send_input`/`stdin_data`) | wired e2e or zero references | — | **done (removed)** — mid-run stdin was fully unwired: the `/input` endpoint produced to `runinput:{id}` which **nothing consumed**, `send_input` had **no caller**, `stdin_data` was **never passed**, and there was no frontend UI. Our tools run non-interactively (`terraform -auto-approve`, ansible no-prompt); the human-in-the-loop is the approval gate, not stdin. Removed the endpoint + `send_input` + `stdin_data` (console now uses `stdin=DEVNULL`). Zero references remain. `test_rbac_endpoints.py`: endpoint → 404, `CommandConsole` has no `send_input` |
| U8 | SSE contract regression green on Redis bus (reducer unchanged) | `backend/tests/test_event_bus_redis.py` (full-vocabulary parity), frontend `tests/sse.test.ts` + `e2e/core-flow.spec.ts` (unchanged) | vitest + Playwright streaming green | ⑫ | **done** — new parity test drives the **full** SSE vocabulary (run/step/token/analysis/params/confidentiality/console/interrupt/error/done) through both the in-memory `RunChannel` and the `RedisChannel` and asserts **byte-identical JSON frames** — the frames are exactly what the reducer keys off, so it needs no change. Reducer/parser untouched: **vitest 28 green**; **Playwright `core-flow` 5 green** incl. "sending a message streams a live run into the per-message timeline". Live multi-worker-on-Redis-bus demo reserved for the Phase-2 exit gate |
| P16 | DevOps CI poll-to-completion | `agents/devops.py`, `tools/github.py` (`find_dispatched_run`/`get_run`/`poll_run_to_completion`; removed `latest_run_status`) | polls the dispatched run id to completion | — | **done** — `workflow_dispatch` returns no run id, so the agent now IDENTIFIES the run it created (newest `workflow_dispatch` run on the branch created ≥ dispatch time, tz-safe) and POLLS **that run id** to completion, reporting the real conclusion. A `failure` conclusion fails the pipeline (`ENSURE_IMAGE_EXISTS` → outcome failed); a run not yet visible is reported honestly ("dispatched"), never faked success; a timeout leaves status non-completed (no fake success). `test_devops_ci_poll.py` (5): poll loops until completed + emits progress, timeout stays honest, execute polls the dispatched id on success, fails on `failure`, honest label when not visible. Live poll against real Actions pending a GITHUB_TOKEN |

**Phase 2 exit gate:** worker-kill mid-apply recovers exactly once · multi-worker streaming + reconnect · turn-20 recall in UI · real failed policy check · real Traces tab · honest model menu. — **status: pending**

### Phase 3 — Intelligence layer

| ID | Item | Files touched | Acceptance test | UI | Status |
|----|------|--------------|-----------------|----|--------|
| D3 | World Model + Reconciliation Engine: schema, ingestion, drift job, orphans, `impact_of` in destroy path | new `graph_db/world_model.py` (org-scoped `Resource` nodes + `DEPENDS_ON` edges, `impact_of`, `ensure_schema`), new `agents/drift.py` (reader seam, pure `detect_drift`, dedup'd notifications, orphan sweep), `agents/inventory.py` (ingestion on apply), `agents/cloudops.py` (`_world_model_impact_check` on the destroy card), `agents/reconciler.py` + `settings.py` (`AEGISOPS_DRIFT`, default off), `main.py` (schema at startup) | manual SG drift → notification; depended-on destroy warns; orphan found | ⑥⑪ | **done** — `test_world_model.py` (9): edges from REAL inputs/outputs only (pure extraction, placeholders skipped), `impact_of` names active dependents (by id or name), org-scoped, destroyed dependents drop out, external parents get honest `status='external'` stubs, destroy-card check fails with named dependents / passes only when consulted / **pending when the graph is down (never a silent pass)**. `test_drift.py` (8): curated-field comparator (order-insensitive lists, never invents unrecorded fields, SG-ports case), fake-reader pipeline → org-scoped notifications (drift amber / deleted-outside red / orphan) with 24h Redis dedup + world-model annotation (best-effort — a down graph doesn't drop findings), readerless types skipped not guessed. Bell UI already renders `GET /notifications`. Live sweeps = **DLV-10**; real `Ec2Reader` ships (cred-gated) |
| DEP | Dependency closure resolution (strict order: named → world model → stated default → create-first DAG) | new `agents/dependency.py` (`Slot` map of the modules' REAL dependency fields, `resolve_closure`), `agents/cloudops.py` (wired after validation: ask → clarification with candidates; dag → ordered proposal + `state["goal_dag"]` for U6; complete → enriched inputs + provenance on the card's defaults) | tests (a) VPC→EC2 DAG; (b) RG→storage DAG; (c) EKS offered existing VPCs; (d) two VPCs → asks | ⑥⑩ | **done** — `test_dependency.py` (11): **(a)** "in a new vpc" → `[aws.vpc, aws.ec2]` parents-first, child wired to real outputs (`subnet_id ← public_subnet_ids[0]`); no-new + no-VPCs → stated default (stated, never silent); **(b)** storage w/o RG → `[azure.resource_group, azure.storage]`, RG addressed by name; one RG → used + noted; **(c)** EKS: one VPC → `vpc_id`+`subnet_ids` filled from its RECORDED outputs; two → offered with "or say new"; VPC without recorded subnets → asks naming the underivable field (never guesses); **(d)** EC2 with two VPCs → asks; one VPC → its real subnet used over the default. Named always wins; slot-less templates pass through untouched |
| U6 | Governed Executive Loop: `execute_governed_step`; goal-DAG approval card; per-step timeline; deviation re-approval; bounds; `AEGISOPS_EXEC_LOOP` | new `agents/exec_loop.py` (`plan_goal_dag`/`execute_goal_dag`/`execute_governed_step`/`resolve_wires`/`validate_dag`), `agents/cloudops.py` (DEP dag branch → loop when flag on), `agents/execute.py` (dispatch), `agents/state.py` (`goal_dag` channel), `settings.py` (`AEGISOPS_EXEC_LOOP`, default off), `frontend/components/Workspace.tsx` (goal-DAG card: ordered steps, per-step plan summary or honest "plans after parent") | VPC+EC2 one approval both applied in order; EFS replan → re-approval; bounds tested | ⑩ | **done** — `test_exec_loop.py` (10): **one approval, both steps applied IN ORDER** with the child wired to the parent's REAL outputs (`subnet_id ← public_subnet_ids[0]`); **interrupt-replay never re-applies a done step** (A1 claim + stored result — LangGraph replays node bodies on resume); step failure → honest partial report ("steps 1 applied; step 2 failed: …", later steps never attempted); **replan = deviation → fresh approval interrupt showing was/now** (approved → revised step runs; rejected → halt); `MAX_REPLANS_PER_STEP=1` bound enforced; `MAX_STEPS=5` + approved-modules-only governance; **a step whose real plan fails a policy check halts, never auto-applies**. Per-step timing (`loop_step_N_<key>`) feeds the timeline/Traces. Live VPC→EC2 DAG e2e in the UI = Phase-3 exit gate (DLV-12) |
| PR-3 | CANCEL — user-facing run cancellation (pairs with U6; spec `docs/prod/PROD_READY_PACK.md`): pre-approval Cancel → cooperative cancel → terminal `cancelled` ("nothing was changed", plan file cleaned per PR-1, idempotency released); awaiting-approval = Reject (no new path); DAG execution: halt-after-current-step, NEVER kill mid-apply, honest partial outcome; authz initiator/approver org-scoped, audited. BINDING: `cancelled` becomes a first-class TERMINAL status everywhere (reconciler scan sets, B5 backstop, UI badges, overview counts, AGENT_RUNS labels — grep every status-set literal) | `api/chat.py`, runner/supervisor, exec_loop, frontend | cancel during plan → cancelled + no side effects; cancel mid-DAG → current step completes, next never starts, honest partial | ⑩ | **done** — `test_pr3_cancel.py` (8): cooperative cancel flag (`run:<id>:cancel`, Redis) + `request_cancel`/`is_cancelled`/`clear_cancel`; POST `/runs/{id}/cancel` (authz: initiator OR approver, org-scoped; terminal runs are a no-op); pre-approval → `signal_cancel` cancels the live drive → `_mark_cancelled` terminal (nothing changed, plan cleaned, flag released, `CancelledError` re-raised); DAG → **halt-after-current-step** check at the loop boundary (never mid-apply) → honest `_partial_outcome` "steps 1–2 applied, cancelled before step 3" as terminal `cancelled`; awaiting_approval flips directly; **`cancelled` is first-class terminal everywhere** — reconciler EXECUTING_STATES excludes it, `_force_terminal` never overwrites it, artifacts overview/trace render it (amber, never "failed"), AGENT_RUNS status label passes it through. Also carries the two latency-test console-kwarg fixes (PR-2) + the PR-1 terminal-tuple test update. Live = DLV-33 |
| INV | Read-only investigation agents (SRE triage, discovery); sub-agents allowed here only; deepagents here only | new `agents/investigation.py` (`ToolRegistry` asserted-read-only + frozen, `Investigator` budget-bounded + `spawn()`, `default_registry` of real reads), `agents/sre.py` (triage K8s evidence through the investigator) | agents hold read-only tools (asserted); no mutation delegation | ⑧ | **done** — `test_investigation.py` (21): registration REJECTS 13 mutation-shaped tools (`restart_/apply_/destroy_/patch_/set_/dispatch_/upsert_`…); registry frozen after build (a running investigation can't grow its surface); **default registry holds ONLY reads, asserted by IDENTITY against the K8s mutating methods**; unknown tool → refused, never guessed; `MAX_CALLS=8` budget enforced; **`spawn()` shares the same frozen registry + the same budget — a sub-agent is never wider**; failed reads are evidence not crashes; SRE triage collects deployments through the investigator (console: "investigation (read-only)"). deepagents (decision 13) would plug in as a director over this same registry — the tool surface is the boundary either way |
| — | **MODSEED** (authoritative spec: `docs/modseed/MODSEED_FINAL.md` — six new modules 14→20, seven enhancements, backcompat B1–B5, seamless-operation contract). Non-negotiables: flat workspace layout; multi-file .tf preferred; a module EXISTS only when registered — dir + FULL registration (templates key+synonyms · schema · params · REAL U1 policy predicate · DEP Slots · inventory+destroy · world-model fields · C1 plan-assertion + faked-runner tests) land in ONE commit; registry↔disk test green; no backend blocks / hardcoded regions / provider bumps; NEVER import tfvars, `backend "pg"`, the source LB folder, Ansible-mutation playbooks, world-open ingress, password-vars, multi-instance counts. **Canary gate (B5) after EVERY MODSEED commit**: pre-MODSEED e2e flows stay green. Owner decisions: NO VM start/stop (options presented at MODSEED end only); ALB = MPP first drafted proposal. **STOP with the evidence table after MS-1..6; STOP again after MS-13** | — | — | — | pending |
| MS-1 | gcp-vpc (`gcp.vpc`; syn network/vpc): custom-mode network + regional subnets (secondary pods/services ranges recorded for future GKE DEP) + private_ip_google_access + optional flow logs + Cloud Router/NAT (ERRORS_ONLY) + internal firewall scoped to subnet CIDRs ONLY (no admin/SSH rules — the VM module owns admin ingress). Required: name (project auto). Outputs: network id/name/self_link, subnet ids+cidrs, secondary range names | new workspace + full registration (templates+synonym `network`, `GCPVPCInputs` w/ RFC1918 CIDR validation, params, `_gcp_vpc_policy` over plan JSON) | policy: custom-mode on, ≥1 subnet; seamless-contract test; canary green | ⑥ | **done** — `test_modseed_ms1_gcp_vpc.py` (9): NEW registry↔disk consistency test (every workspace dir ↔ registered template, allowlist demo-null/promoted-*; both directions); C1 source invariants (custom mode, no 0.0.0.0/0, NAT ERRORS_ONLY, private_ip_google_access, no backend block, fw scoped to var.subnet_cidrs, no admin ports, region from var); REAL `terraform fmt -check` + `init -backend=false` + `validate` green on the dir; synonym routing (`gcp`+`network` → gcp.vpc, never cross-cloud); schema rejects public/malformed/empty CIDRs; params ask only `name`; policy passes on a custom-mode plan and FAILS on auto-subnet/zero-subnet plans (pending when no plan); seamless-contract integration (faked runner, live PG/Redis/Neo4j): plan → real checks on the card → apply → inventory row + world-model node. Live apply/destroy → DLV-15 |
| MS-2 | azure-vnet (`azure.vnet`; syn vnet/network): VNet + subnets (10.20.0.0/16 default) + NAT gateway (+static IP+assoc) + public/private route tables (name-based assoc); RG handling like azure-vm. NO world-open NSG (omit or default-deny). Required: name. Outputs: vnet id/name, subnet ids, RG | new workspace + full registration (template + `network` synonym, `AzureVNetInputs` RFC1918-validated, params, `_azure_vnet_policy` over plan JSON incl. a NO-NSG check, azure.vnet RG DEP slot) | policy: ≥1 subnet, RFC1918; seamless test; canary green | ⑥ | **done** — `test_modseed_ms2_azure_vnet.py` (8): C1 source invariants (NO `azurerm_network_security*` at all, no 0.0.0.0/0 literal — even the public route table relies on Azure system routes, NAT gateway + name-based route-table association present, RG semantics identical to azure-vm, no backend block, no admin ports, azurerm pinned ~>3.110); REAL fmt/init/validate green; synonyms (`azure`+`network`/`vnet`, never cross-cloud); schema rejects public space; params ask only `name`; RG DEP-slot family (existing RG used / default stated); policy passes on a good plan, FAILS on public-space + NSG-carrying plans, pending without a plan; seamless-contract integration → inventory row + world-model node. Live lifecycle = DLV-16 |
| MS-3 | aws-nlb (`aws.nlb`; syn load balancer/lb/nlb): network LB (cross-zone) + TCP TG (health TCP/30s/3/traffic-port) + TCP listener + auto egress-only SG when none given; deletion_protection var (default true when env=Production); target_port default 80. DEP Slots: vpc_id+subnets ← aws.vpc recorded public_subnet_ids (two VPCs → ask; none → create-first DAG via the exec loop, ONE approval). Card: DNS name + attach-targets-to-<tg arn> note | new workspace + full registration + DEP slots (aws.nlb → vpc_id + subnets ← the VPC's recorded `public_subnet_ids`) + NEW `templates.apply_env_defaults` hook (deletion_protection ON for env=Production, stated on the card, explicit choice wins) | DEP one/two/none cases; seamless test; canary green | ⑥⑩ | **done** — `test_modseed_ms3_aws_nlb.py` (11): C1 source invariants (network type, cross-zone on, TCP health 30s/x3, **auto SG has NO ingress block** — the single 0.0.0.0/0 is the egress route, no backend block, aws pinned ~>5.60); REAL fmt/init/validate green; synonyms (lb/load_balancer/loadbalancer/nlb, never cross-cloud); listener_port defaults to target_port; params ask only `name`; env default verified for Production/Staging/explicit-wins; **DEP: one VPC → vpc_id + subnets filled from RECORDED outputs (provenance on card); two → offered; none → create-first DAG [aws.vpc → aws.nlb] wired to real outputs**; policy passes on a good plan and FAILS when the plan quietly drops deletion protection; seamless integration: card carries DEP provenance + env default → apply → inventory row + **world-model DEPENDS_ON edge to the VPC** (impact_of names the NLB). `attach_targets_note` ships as a real output. Live lifecycle = DLV-17 |
| MS-4 | aws-kms (`aws.kms`; syn kms/key/encryption key; secrets→kms): key (deletion_window 30, rotation ON) + alias `alias/<name>` + key policy (root via caller identity + allowed_services default [secretsmanager, rds] with Decrypt/DescribeKey/CreateGrant). Secret VALUES out of scope forever (never chat-supplied). Destroy card: scheduled-deletion window, not immediate | new workspace + full registration + NEW `WorkflowTemplate.destroy_note` seam (honest per-module deletion semantics surfaced on the destroy card — MS-6 reuses it) | policy: rotation on, deletion_window ≥7; destroy-semantics card; seamless test; canary green | ⑥ | **done** — `test_modseed_ms4_aws_kms.py` (9): C1 source invariants (rotation var, bounded window incl. a module-level validation, `alias/<name>`, root-admin-via-caller-identity policy, Decrypt/DescribeKey/CreateGrant for allowed services, **no password/secret_string anywhere — keys, never secrets**, no backend block, aws ~>5.60); REAL fmt/init/validate; synonyms kms/key/encryption_key/**secrets→kms**; schema rejects windows <7/>30; params ask only `name`; policy passes on a good plan and FAILS on rotation-off/short-window plans (input fallback pre-plan); **the destroy card's reasoning includes "Deletion semantics" — scheduled-deletion window, NOT immediate** (integration through the real destroy-plan path); seamless contract → inventory row with `rotation_enabled` recorded (day-2 "what's the rotation on app-secrets" answers from it) + world-model node. Live = DLV-18 |
| MS-5 | azure-keyvault (`azure.keyvault`; syn key vault/kv/vault): vault (standard, soft-delete ≥7, purge_protection default true, network_acls bypass AzureServices; default_action stated on the card when Allow) + current-SP access policy + optional additional-policies map + optional RSA-2048 keys map. RG via the existing resource_group Slot pattern. No secret values. Outputs: vault_id/uri | new workspace + full registration (template + key_vault/kv/vault synonyms, `AzureKeyVaultInputs` bounded, params, `_azure_keyvault_policy`, RG DEP slot, `destroy_note` soft-delete semantics, Allow-action card note via the defaults hook) | policy + card statements; seamless test; canary green | ⑥ | **done** — `test_modseed_ms5_azure_keyvault.py` (10): C1 source invariants (standard SKU, bounded soft-delete, purge var, AzureServices bypass, current-SP policy via real client config, RSA-2048 keys only, **no secret_value / azurerm_key_vault_secret anywhere — the vault, never secrets**, RG semantics like azure-vm, azurerm ~>3.110); REAL fmt/init/validate; synonyms; schema rejects <7-day soft delete / bad action / short names; params ask only `name`; **network_default_action=Allow is STATED on the card** (Deny stays silent); RG DEP-slot default stated; destroy_note: soft-delete retention + purge-protection block; policy passes on a good plan and FAILS on short-retention/purge-off/no-bypass plans; seamless contract → inventory row with `vault_uri` recorded + world-model node. Live = DLV-19 |
| MS-6 | gcp-kms (`gcp.kms`; syn kms/keyring/key): key ring + crypto key(s) (rotation 90d, ENCRYPT_DECRYPT, SOFTWARE; default one key `<name>-key`) + encrypter/decrypter IAM members var. Required: name (project auto). Destroy card MUST say: GCP key rings are not deletable — destroy removes key versions/IAM only | new workspace + full registration (template + keyring/key/encryption_key/**secrets→kms** synonyms, `GCPKMSInputs` rotation_days 1–365, params, `_gcp_kms_policy` rotation/SOFTWARE/ENCRYPT_DECRYPT, `destroy_note` ring-permanence) | destroy-semantics card asserted; seamless test; canary green — **STOP: evidence table before MS-7** | ⑥ | **done** — `test_modseed_ms6_gcp_kms.py` (9): C1 source invariants (rotation_period derived `"${var.rotation_days * 86400}s"` — days→seconds, no magic string; SOFTWARE + GOOGLE_SYMMETRIC_ENCRYPTION; ENCRYPT_DECRYPT; setproduct IAM → cryptoKeyEncrypterDecrypter; "NOT deletable" documented in-module; **no google_secret_manager / secret_data — keys, never secrets**; no backend block; google ~>5.40); REAL fmt/init/validate; synonyms kms/keyring/key/encryption_key/**secrets→kms** (aws `key` still → aws.kms — per-cloud coexistence asserted); schema defaults rotation 90d, rejects rotation_days 0; params ask only `name`; policy passes on a good plan (7776000s/SOFTWARE/ENCRYPT_DECRYPT) and FAILS all three checks on ASYMMETRIC_SIGN/HSM/no-rotation; **the destroy card's "Deletion semantics" says rings are NOT deletable — versions/IAM only** (integration through the real destroy-plan path, reusing the MS-4 `destroy_note` seam); seamless contract → inventory row `resource_type == "kms"` with `rotation_days` recorded (day-2 rotation answers) + world-model node. Live = DLV-20 |
| MS-7 | aws-rds multi-engine (postgres/mysql/mariadb) + latest-engine-version data source + dedicated SG with MANDATORY allowed_cidr (never 0.0.0.0/0) + subnet group + engine-aware ports/log-exports + sensitive connection-string outputs; KEEP manage_master_user_password=true | enhancement (B1 no-op re-plan gate; B2 old-behavior defaults) | **B1: a pre-enhancement resource re-plans to ZERO changes from its STORED inventory inputs**; canary green | ⑥ | **done** — `test_modseed_ms7_aws_rds.py` (8): engine validation postgres/mysql/mariadb + engine-aware port/exports/DSN maps; `aws_rds_engine_version` data source count-gated (resolved only for "latest" or logging family — no credential-less plan ever touches it); **`0.0.0.0/0` appears nowhere in the module** + schema AND module validation reject any `/0`; conditional SG/subnet-group; query-logging parameter group via `for_each` (count breaks checkov's graph edge — for_each keeps CKV2_AWS_30 provable); **B1 gate is a REAL `terraform plan`** (schema-validated old-shape stored inputs, credential-less via a test-only `*_override.tf`, removed in finally) asserting EXACTLY `{aws_db_instance.this: [create]}` with exports/param-group/SG/subnet-group/engine_version all null and every old attribute equal; **B2 at the schema level** (enable_log_exports=False etc.) while the MODULE defaults stay secure (checkov evaluates module defaults — this is what lets the waivers die); **first proof: CKV_AWS_129 + CKV2_AWS_30 waivers REMOVED** (aws-rds checkov config carries six non-MS skips only), CKV2_AWS_60/AVD-AWS-0077 re-justified without MS tags; policy `_rds_policy` gained "DB security group scoped (no /0)" (plan-aware + input fallback) and "Engine-aware log exports"; **stale-waiver guard shipped** (`test_scanner_waiver_guard.py` + FIX.md ro-mount into api-test): any config referencing a done MS item = red suite. Live = DLV-21 |
| MS-8 | azure-postgres → azure.db multi-engine (postgresql/mysql/mssql) + optional HA, geo-redundant backup, delegated subnet/private DNS; KEEP generated random_password; `azure.postgres` stays a synonym; **workspace dir name UNCHANGED (B3)** | enhancement | B1 no-op re-plan; B3 dir-immutability + day-2 destroy test; canary green | ⑥ | **done** — `test_modseed_ms8_azure_db.py` (9): three engine families each `for_each`-gated on `var.engine` (postgres = the pre-enhancement family, mysql flexible w/ storage_mb→size_gb, mssql server+S0 database w/ TLS-1.2 + honest preconditions rejecting HA/delegated-subnet for mssql); **`moved` blocks migrate the old unkeyed addresses to `this["postgresql"]`** so real state re-plans as a no-op rename (B1 for live resources); **B1/B2 gate = COMMITTED native `terraform test`** (`tests/b1_backcompat.tftest.hcl`, mock providers, offline — azurerm acquires AAD tokens eagerly so the aws-style fake-creds plan is impossible): 6 runs green — old-shape inputs render EXACTLY the old postgres plan (public path, no HA, geo false, fw sentinel kept), each engine renders only its family, HA renders ZoneRedundant, private access flips public off + removes the fw rule, mssql+HA expect_failures; generated `random_password` KEPT unconditional; schema `AzureDBInputs` (B2 old defaults; postgres→postgresql shorthand; mssql/HA + subnet-pairing validators; storage floor kept) with `AzurePostgresInputs` alias so old imports live; **`azure.db` template (workspace UNCHANGED azure-postgres) + `by_key("azure.postgres")` alias + synonyms postgres/postgresql/database/db/sql/mysql/mssql/sqlserver→db** (routing tests updated deliberately, B4-recorded); **B3 day-2 destroy proven end-to-end**: an old-shape inventory row (resource_type "postgres", workspace azure-postgres) resolves via synonym→azure.db, the runner takes the ROW's workspace, old inputs validate through the new schema at B2 defaults; policy `_azure_db_policy` engine-aware (keeps the pg version check verbatim; mssql TLS-1.2 plan-aware; HA/private-pairing checks); DEP RG slot moved to azure.db; **waiver removed: CKV_AZURE_136** (module defaults geo-backup ON; schema keeps old behavior) + CKV2_AZURE_26/57 re-justified without MS tags (sentinel-rule/option-now-exists) + 10 net-new mssql/mysql-family findings triaged in the ledger; azure-postgres scans green (checkov 6/0, tfsec clean). Live = DLV-22 |
| MS-9 | gcp-cloudsql: optional private-VPC-peering + backup/PITR + maintenance window + query insights + ssl_mode + deletion_protection var + optional CMEK encryption_key_name (DEP Slot on gcp.kms: existing ring → offered; none → optional, never forced) | enhancement | B1 no-op re-plan; CMEK slot cases; canary green | ⑥⑩ | **done** — `test_modseed_ms9_gcp_cloudsql.py` (8): every option variable-driven with **schema B2 old-defaults incl. the legacy world-open `all` authorized network preserved verbatim** (value AND historical name — no in-place rename on re-plans) while the MODULE defaults are secure (no authorized networks, backups+PITR on, ssl_mode ENCRYPTED_ONLY, the full 10-flag pg observability set as the `database_flags` map default, query insights on); private_network drops the public IP + networks entirely; deletion_protection var-driven (destroys stay approval-gated); **CMEK DEP slot on gcp.kms** (required=False, `attr:key_ids[0]`, stated default "Google-managed encryption" — offered when a ring exists, never forced); **B1/B2 gate = committed native `terraform test`** (mock providers, 5 runs green; optional+computed attrs asserted through their SET paths); policy: **"No world-open authorized networks" FAILS VISIBLY on the legacy default** (the approver sees it), private/backup/CMEK stated; **waivers REMOVED: CKV_GCP_14 + CKV_GCP_11 (checkov) and the tfsec pg-flag family + backups + world-open-network (6 excludes)** — tfsec evaluates the module's secure defaults; the checkov flag family is re-justified as a documented SCANNER LIMITATION (checkov cannot expand dynamic blocks inside `settings` — the committed terraform test asserts all 10 flags render, regression-proof); CKV_GCP_6/AVD-GCP-0015 re-justified (provider 5.x replaced `require_ssl` with `ssl_mode`; scanner tracks the removed attr); CKV_GCP_60/AVD-GCP-0017 re-justified (sandbox access path; the private option ships). Live = DLV-23 |
| MS-10 | aws-ec2: optional SSM+CloudWatch instance profile (enable_ssm default FALSE per B2); card adds "Session Manager access available" when on | enhancement | B1 no-op re-plan; card note; canary green | ⑥ | **done** — `test_modseed_ms10_aws_ec2_ssm.py` (6): IAM role (`AmazonSSMManagedInstanceCore` + `CloudWatchAgentServerPolicy`) + instance profile, the whole chain `for_each`-gated on `enable_ssm` (for_each, not count — scanners follow the graph, which is what lets the waiver die); `iam_instance_profile = one(values(...))` wires null by construction when off; **schema default FALSE (B2, verbatim per spec) / module default TRUE** (secure-observable bare use); **B1 gate = committed native `terraform test`** (mock provider + an `override_data` for the default-subnet discovery — bare mocks return empty lists; the workspace's live `.terraform` carries an A3 backend pointer so tests run under an isolated `TF_DATA_DIR`): old-shape inputs render ZERO IAM/SSM resources, enable_ssm renders the full chain with both managed-policy ARNs asserted; card states **"Session Manager access available"** via `_ec2_policy` when on (IMDSv2/encryption checks untouched); params offer the bool (required set unchanged); **waiver REMOVED: CKV2_AWS_41** — aws-ec2 checkov 24 passed / 0 failed; tfsec clean. Live = DLV-24 |
| MS-11 | eks-provision: eks_mode auto or standard (default standard per B2) — auto → API auth mode, compute_config (general-purpose pool), elastic-LB + block-storage configs, auto-mode IAM set; standard → current node-group path; card states the mode | enhancement | B1 no-op re-plan (standard path unchanged); canary green | ⑥ | **done** — `test_modseed_ms11_eks_auto_mode.py` (5): mode wiring via root locals passed to the registry module — **standard renders the EXACT old inputs (nulls = the module's own defaults untouched; the pre-enhancement `app` node group verbatim)**, auto forces `authentication_mode="API"` + `cluster_compute_config {enabled, node_pools=["general-purpose"]}` + `bootstrap_self_managed_addons=false` + zero node groups (the registry module wires the elastic-LB/block-storage configs and attaches the auto-mode IAM policy set — its own tested product; the `~> 20.8` constraint already floats to the 20.31+ releases that carry Auto Mode, no pin change); **B1 gate = committed native `terraform test` with `override_module`** (the registry module is output-mocked; OUR conditional wiring is what's asserted — `tolist()` on `keys()` comparisons, HCL list≠tuple); schema `eks_mode` validated standard|auto, **default standard (B2 verbatim)**; card states the mode via the new "Cluster mode" `_eks_policy` check; params offer the choice (required set unchanged); no waiver changes (eks CKV_TF_1 was never MS-tagged); scans green (checkov 2/0, tfsec clean). Live = DLV-25 |
| MS-12 | gcp-gce: shielded_instance_config, OS Login, preemptible/spot (maintenance implications stated on the card), optional least-scope service account (all default off per B2); KEEP generated SSH key + one-time reveal. **B4: new DEP Slot gcp.vm→network changes placement BY DESIGN — update affected DEF/U4/plan tests deliberately + record each change in PROGRESS.md; never weaken a test to pass** | enhancement + DEP slot | B1 no-op re-plan; B4 changes recorded; canary green | ⑥⑩ | **done** — `test_modseed_ms12_gcp_gce.py` (8): every option variable-driven with schema B2 old-defaults (public IP ON, network "default", everything else off) while the MODULE defaults are secure (shielded on, project-wide keys blocked, NO public IP); spot renders SPOT provisioning + no automatic restart and **the card states "may be STOPPED by GCP at any time … no automatic restart"**; OS Login states the generated key becomes unused; least-scope SA = logging+monitoring writes only; the `network` var drives the instance AND both firewalls (asserted ×3); **B4 gcp.vm→network DEP slot** (required=False, creator gcp.vpc, value_from=name, stated default "the project's default network") — DEF's default-network row became CONDITIONAL (deliberate, recorded: a slot-filled/user-named network is a real placement; only the default placement is flagged), no other test needed weakening; **B1 gate = committed native `terraform test`** (5 runs green incl. the exact old shape: access_config present, metadata = ssh-keys ONLY, no shielded/scheduling/SA blocks); KEPT: generated SSH key + one-time sensitive reveal; **ALL gcp-gce scanner waivers DELETED — both config files removed** (bare scans: checkov 19/0, tfsec clean; CKV_GCP_39/40/32/38 + AVD-GCP-0030/0031/0033/0041/0045 all pass on module defaults). Live = DLV-26 |
| MS-13 | azure-aks (optional, last): Log Analytics + OMS agent, network_policy=calico, azure_policy_enabled var (defaults off per B2). **B4: azure.vm→vnet Slot likewise.** — **STOP after MS-13 before MPP** | enhancement + DEP slot | B1 no-op re-plan; canary green | ⑥⑩ | **done** — `test_modseed_ms13_azure_aks.py` (9): Log Analytics workspace (PerGB2018, 30-day retention) + OMS agent, calico-on-kubenet network profile, `azure_policy_enabled` — all variable-driven, **schema B2 defaults OFF / module defaults ON** (which is what kills the waivers); the RBAC pin stays; **B1 gates = committed native `terraform test` in BOTH workspaces** (aks 4 runs: exact old shape / workspace+OMS / calico / policy; azure-vm 2 runs: old shape creates its dedicated vnet+subnet, a slot-filled subnet skips both and lands the NIC in the existing network — with `moved` blocks covering the count-index migration for real state); **B4 azure.vm→vnet DEP slot** (required=False, creator azure.vnet, `attr:subnet_ids[0]`, coexists with the RG slot) — no existing test needed changing (azure DEF rows key on the RG only); cards state each add-on (`_azure_aks_policy`); params offer the three options (required sets unchanged); **waivers REMOVED: CKV_AZURE_4 + CKV_AZURE_7 + CKV_AZURE_116 and tfsec AVD-AZU-0040 + AVD-AZU-0043** (true-bare scans on a config-free copy prove it — checkov auto-discovers a directory's own `.checkov.yaml`, so in-place scans are never bare; 12 sandbox-posture checkov skips + the authorized-IPs tfsec exclude remain, none MS-tagged). Live = DLV-27 |
| PR-1 | TFHYGIENE — Terraform disk lifecycle (spec `docs/prod/PROD_READY_PACK.md`): delete the run's .tfplan at terminal states (the reviewable record persists in runs.plan_json); reconciler sweep removes stray plan files >7d — **BINDING: B3 resume applies the SAVED plan file → only terminal-state runs' files are deletable and the stray sweep checks run status first (awaiting_approval may wait days; its plan file is NOT stray)**; destroyed-workspace pruning by the SWEEPER only (destroyed inventory + empty `terraform state list` >7d; logged, never silent, never inline at destroy, no chat trigger; remote state relies on bucket versioning); TF_PLUGIN_CACHE_DIR covers all MODSEED providers; .gitignore covers .terraform/ everywhere | `tools/terraform.py`, reconciler | N runs → terminal → zero stray .tfplan; destroy → prune after threshold → day-2 on OTHER resources unaffected | — | **done** — `test_pr1_tf_hygiene.py` (7): (a) terminal runs delete their `.tfplan` — `_cleanup_terminal_plan_files` fires from `_persist_result` (completed/failed) AND `_force_terminal`; awaiting_approval KEEPS its plan (the approved apply needs it); the reviewable record persists in runs.plan_json; (b) reconciler `sweep_tf_hygiene` removes stray plans >7d (BINDING: skips any non-terminal run's plan via keep_run_ids) and prunes a DESTROYED resource's per-state workspace whose terraform state holds ZERO resources >7d — sweeper-only, reads the state FILE directly (no subprocess), refuses a non-empty state, clears the row pointer; NO chat request can trigger a prune; (c) TF_PLUGIN_CACHE_DIR + `.gitignore` (.terraform/ · *.tfplan · *.tfstate) verified (api-test ro-mounts ./.gitignore). Live = DLV-31 |
| PR-2 | LIMITS — concurrency + subprocess timeouts: max concurrent ACTIVE runs per org (default 5) and per user (2); at the limit POST /chat returns an honest 429-style "N runs in progress — retry when one completes" — **BINDING: derive active counts from the EXISTING liveness truth (runs.status non-terminal + fresh run:<id>:hb heartbeats) — never a separate drifting counter; heartbeat-derived counts self-heal like the reconciler**; TerraformRunner per-stage timeouts (init/plan 10m, apply/destroy 45m, env-configurable) — **BINDING: SIGTERM → grace → SIGKILL on the process group; classify the leftover state lock via provider_errors; rely on terraform stale-plan protection; force-unlock guidance in the runbook**; run terminal `failed`, NEVER hang a live worker (heartbeat stays fresh while a subprocess hangs — the reconciler cannot see it today); the exec loop inherits per-step timeout + the whole-DAG budget | settings, `api/chat.py`, `tools/terraform.py`, exec_loop | sleeping fake runner → honest failure within bound; org at limit → clear refusal, no leaked run row | — | **done** — `test_pr2_limits.py` (7): (a) `_active_run_counts` derives ACTIVE runs from the LIVENESS TRUTH (non-terminal status AND a fresh `run:<id>:hb`) — a stale row from a crashed worker self-heals OUT (no drifting counter, no lock-out); awaiting_approval does NOT count; POST /chat refuses at the org (5) / user (2) cap with a 429 BEFORE persisting anything (no leaked row); (b) `CommandConsole.run(timeout=)` per stage (init/plan 10m, apply/destroy 45m, env-configurable), `start_new_session=True` + SIGTERM→10s grace→SIGKILL on the process GROUP (reaps provider children), rc 124 → `_raise_stage_failure` classifies honestly ("exceeded 45m … process group killed … reconciler will reconcile … TF_FORCE_UNLOCK"), the timeout signal lands on the result even without a callback; a sleeping runner fails within bound, never hangs a worker. Live = DLV-32 |
| MPP | Module Promotion Pipeline: draft → fmt/validate → Checkov/tfsec → proposal + review UI → promote | new `agents/module_pipeline.py` + migration `0007_module_proposals` + `ModuleProposal` model, `agents/templates.py` (`register_promoted` + `_PROMOTED` consulted by `select`/`by_key`/`catalog`), `api/modules.py` (5 endpoints; review = approver RBAC; org-scoped 404), `main.py` (rehydrate at startup), `frontend/components/ModuleView.tsx` (proposals panel on Infrastructure: checks, status, Promote/Reject) | drafted module unselectable until promoted; never same-turn execution | ⑥ | **done** — `test_module_pipeline.py` (9): draft validation (bad key / catalog collision / no .tf / path escape); **draft is inert — unselectable via `by_key`/`select` even after propose**; REAL `terraform fmt -check` + `init -backend=false` + `validate` in an isolated dir (green module passes; broken HCL fails validate; unformatted fails fmt); propose requires green checks; **promotion fails CLOSED without a PASSED scan** (checkov→tfsec seam; neither installed here → `unavailable` blocks — live scan = DLV-13); promote materializes `promoted-<key>` under the workspaces dir + registers a runtime template (permissive `name`-required schema, honest `_todo` policy, all gates still apply) and only THEN is it selectable; reject terminal; checks frozen after terminal; `rehydrate_promoted` re-registers from the DB at startup |
| M4 | Per-user/org persistent memory, user-editable, in `build_context` | new `agents/user_memory.py` + migration `0008_user_memory` + `UserMemory` model (partial unique idx per scope), `agents/memory.py` (`build_context` leads with the standing block; router/general/knowledge pass org+user), `agents/cloudops.py` (`_extract_inputs`: deterministic "my usual region" → memory lookup, explicit region still wins; Azure maps to `location`), `api/modules.py` (`GET/PUT/DELETE /memory`; org-wide rows require approver), `frontend` (MemoryPanel on Administration: list/remember/forget; `api.put` added) | "my usual region" honored in a new session | ⑨ | **done** — `test_user_memory.py` (6): set/upsert/delete roundtrip; org-wide row visible to members with the user row winning; org-scoped (no leak); **a brand-new session's FIRST context already leads with "Standing user memory … usual_region: ap-south-1"**; **"in my usual region" resolves the region deterministically without an LLM** (explicit `region=` wins; Azure→`location`); block bounded 600 chars and empty-when-unset (never a fake). All 14 memory regressions green. LLM-paraphrase honoring rides the same context block (key-gated, exercised at DLV) |
| U7 | Retry-with-fix + undo last apply (gated destroy) | `provider_errors.py` (`suggest_retry`), `agents/events.py` (`error(retry=)`), `agents/cloudops.py` (plan+execute failure paths carry the suggestion; `__last_applied__` resolution in `_destroy_resource`), `agents/intent_guard.py` (`is_undo`; undo/revert now explicitly destructive), `agents/router.py` (deterministic undo fast-path, pre-LLM), `agents/inventory.py` (`last_applied`, session-scoped), frontend (`retry` on the error event → "Retry with fix" button → real new turn via `sendText`) | bad-region retry one-click; "undo that" gated destroy | ⑥ | **done** — `test_retry_undo.py` (12): bad-region retry swaps the region IN PLACE in the user’s own message (append when unnamed; per-cloud alternates; never re-suggests the failed region; azure `location:` handled); credentials-expired → same-message retry; unclassified/unfixable → NO suggestion (never invented); the error event carries `retry` only when one exists; `is_undo` matches natural shapes and ignores unanchored sentences; undo/revert count as explicitly destructive (the destroy gate accepts them); **`last_applied` is session-scoped, newest-first, skips destroyed** (nothing active → honest refusal); router routes "undo that" → cloudops destroy `__last_applied__` deterministically, NO LLM. The destroy then runs the full gate: approval card + D3 impact check + destroy-only plan guard. Also fixed en route: a literal backspace (0x08) had been written into the region regex — invisible in review, caught by the smoke test |
| COMP | Honest compound / attach-mount / OS-change handling (Phase-3 exit item) | `agents/cloudops.py` `_comp_intercept`, `agents/router.py` | never silently pick one resource; never fake an attach; never fake an in-place OS swap; suite + canary green | — | **done** — `test_comp_honest_handling.py` (11): (a) a compound ask of ≥2 INDEPENDENT resources joined by a conjunction → an honest one-at-a-time offer naming the order + proceeding with the first on confirmation (never a silent single pick); **the DEP boundary holds** — a dependency-linked ask (`inside`/`within`/`into`/`in a new …`, e.g. DLV-12 “a VPC and an EC2 inside it”) falls through to the DEP create-first DAG untouched; (b) attach/mount of object storage onto a VM → creation offered, but mounting is stated plainly as IN-GUEST config outside AegisOps' Terraform scope, with the gcsfuse / s3fs / blobfuse2 one-liners — never a pretend attach, never cross-cloud output wiring; (c) an OS-change on an existing VM → guarded refusal (modify = update-in-place; OS swap = replace) + the gated destroy→fresh-create path, noting Windows lives on azure.vm/aws.ec2 when the target is the Linux-only gcp.vm; router prompt keeps all three OFF destroy (destroy reserved for explicit destroy/delete/remove/terminate; OS-change named as modify). Live = DLV-38 |
| MOD | Modify beyond ports: S3 lifecycle/versioning, RDS scaling, tags | `_modify_resource`, schemas, modules | in-place guard + gate per new type | ⑥ | **done** — `test_mod_day2.py` (11): `_extract_modification` (LLM-first + honest regex fallbacks) + `_MODIFY_CAPS` capability map + `_apply_modification` (union/merge, never replace) generalize the day-2 path — **aws.s3 versioning/lifecycle/tags · aws.rds instance_class/allocated_storage/tags · aws.ec2 ports/power/tags · gcp.vm ports/power · azure.vm ports**; every modify keeps the SAME gates (approval interrupt, `plan_guard` in-place check, policy re-run over the real plan, per-resource state workspace); **owner Option A power state: Terraform-encoded** — `aws_ec2_instance_state` (for_each-gated) + GCE `desired_status`, NEVER an SDK call, `power_state=""` default = unmanaged so existing instances' re-plans are unchanged (B1); **Azure power gets the honest answer verbatim** ("use the portal; create/modify/destroy fully supported here") with no plan and no SDK fallback; router prompt: start/stop is ALWAYS modify, never destroy; unsupported changes get the honest per-type capability list; committed terraform-test gates extended deliberately (aws-ec2 2→4 runs, gcp-gce 5→6, aws-s3 NEW 3 runs incl. the no-lifecycle B1 shape — expiry is always an explicit user choice, never a module default); s3 lifecycle rule ships `abort_incomplete_multipart_upload` (CKV_AWS_300 clean); **scanner-tool discovery recorded**: ANY expression on `desired_status` makes checkov/tfsec drop the resource's evaluated variable defaults — 3 checkov + 2 tfsec gcp-gce waivers re-added with TOOL-LIMITATION reasons (the MODSEED secure defaults are unchanged and tftest-asserted; MS-12's test updated deliberately). Live = DLV-28 |
| COST | Cost estimation → real policy check + approval card (verify tooling at impl) | new cost module, `agents/templates.py`, card | real $/mo on card; guardrail breach → failed check | ⑥ | **done** — `test_cost_estimation.py` (7): **static provider-pricing catalog per the owner's speed directive (Infracost integration → BACKLOG)** — `agents/cost.py` prices compute/db by size (ec2/rds/gce/azure-vm/azure-db/cloudsql), flat rows (nlb hourly, NAT gateways ×az_count, eks/gke control planes + nodes, aks free CP, kms per-key) and honest zero-with-note for usage-based (s3/gcs/storage/keyvault/rg); every card row says "static catalog estimate", never a quote; unpriced sizes → honest "not in the catalog"; power/spot shape the number (stopped ec2 = EBS only; gcp spot ≈35%); **`AEGISOPS_COST_GUARDRAIL_USD` > 0 adds a REAL policy check that FAILS on breach** (approver sees it) and **fails closed** ("cannot verify") when the estimate is unpriced; wired into BOTH plan paths (create + day-2 modify) via `cost.checks_for` after `template.policy_fn`; card integration proven through the real modify path (t3.medium vs a $5 cap → failed guardrail row in the interrupt payload). Live = DLV-29 |
| P17 | Notify real recipients | `agents/notify.py` | initiator/approver addressed, not sender | — | **done** — `test_notify_recipients.py` (6): the defect was `msg["To"] = notify_from` (the SENDER); `_recipients(state)` now addresses the run's INITIATOR (`state.user.email`) + the APPROVER (`state.approver.email`, carried on the approval resume payload in chat.py — pinned at the source) deduped/empty-safe; the from-address is the LOGGED last-resort fallback ONLY when nobody is addressable; junk addresses ignored; `_send_email` takes an explicit recipients list (To = the stakeholders, From stays the sender); in-app Notification still always persisted. Live = DLV-30 |
| PR-4 | RETENTION — configurable sweeper (extends the reconciler loop; all OFF by default in dev, prod defaults documented): messages/run_steps/notifications archive-or-delete >N days (180) for CLOSED sessions only (embeddings cascade with messages); runs keep the row (audit) but compact bulky plan_json to the summary; **audit_log + approvals NEVER auto-deleted** (partitioning guidance documented); Langfuse retention documented; Neo4j closed contexts >N days pruned — the LIVE world-model resource graph is never pruned. **BINDING: compacted old runs' Traces/Terraform tabs show an honest "compacted per retention policy" note, never empty/fake** | reconciler sweep, `api/artifacts.py` | seeded old data → only eligible rows affected; audit rows untouched | — | **done** — `test_pr4_retention.py` (4): `agents/retention.py` extends the reconciler loop (never a parallel scheduler), ALL OFF by default (dev); msgs/run_steps deleted beyond N days for CLOSED sessions only (embeddings cascade), notifications beyond N days, run plan_json COMPACTED to its summary beyond N days (audit row kept + honest `compacted per retention policy` marker surfaced on the Terraform tab), **audit_log + approvals NEVER referenced**; idempotent (a compacted run isn't re-counted); prod defaults documented in .env.example. Live = DLV-34 |
| PR-5 | BACKUP — Postgres is the ONLY store that must be backed up (Redis = ephemeral coordination, rebuildable; Neo4j = derived mirror — ship a `rebuild_world_model` admin command to prove it; TF remote state = bucket versioning, verify in the A3 backend docs): scheduled pg_dump (or WAL archiving) + RESTORE_RUNBOOK.md + a TESTED restore drill (fresh Postgres from backup → app boots → sessions/runs/inventory intact → day-2 on a pre-backup resource works; an untested backup is not a backup). **BINDING: the dump includes the LangGraph checkpoint tables — the drill must PROVE an awaiting_approval run survives restore and can still be approved; note the langfuse DB separately; realm-export.json is the Keycloak backup — keep it current (S0 modified it)** | compose/scripts, runbook, admin command | the restore drill executed once, recorded in PROGRESS.md with evidence | — | **done** — `test_pr5_backup.py` (3): `infra/backup/pg_backup.sh` (custom-format pg_dump + retention) + `docs/RESTORE_RUNBOOK.md` (exact restore steps; Postgres is the ONLY must-back-up store, stated explicitly) + **`python -m app.admin rebuild-world-model`** proving Neo4j is a derived mirror — `world_model.rebuild_from_inventory` reconstructs the live graph from Postgres inventory with NO cloud read (test wipes the graph, rebuilds, node reappears); `docs/TF_FORCE_UNLOCK.md` for PR-2b lock recovery. The live restore DRILL is DEFERRED (DLV-35 — needs a real backup + restore target with the owner; never marked done from a dry read). Live = DLV-35 |
| PR-6 | ALERTS — `infra/prometheus/alerts.yml` + Alertmanager wiring docs: stranded-run count >0 for 10m · reconciler sweep failures · drift/orphan findings (warning) · API 5xx rate · run failure-rate spike · event-bus publish errors · disk usage on the TF/plugin volumes (ties to PR-1) · Langfuse export failures; each rule carries a runbook line ("what to check first"); Grafana panels optional. **BINDING: emit the missing gauges (aegisops_stranded_runs, reconciler_errors, drift_findings) from the reconciler/drift sweeps as part of this item, O3-style** | alerts.yml, `metrics.py`, reconciler/drift | `promtool check rules` clean; one alert fires in a simulated condition | — | **done** — `test_pr6_alerts.py` (5): `infra/prometheus/alerts.yml` (7 rules — stranded runs, reconciler sweep failures, drift/orphan findings, API 5xx rate, run failure spike, dependency down, event-bus errors), each with a `runbook` annotation; loaded via `rule_files` in prometheus.yml + mounted in compose; **new gauges** `aegisops_stranded_runs` / `aegisops_reconciler_sweep_failures_total` / `aegisops_drift_findings` published by the reconciler each sweep; rules reference only real exposed metric families (asserted against the live registry); promtool lint runs when available (skips honestly otherwise). One alert firing in a simulated condition = DLV-36. Live = DLV-36 |
| PR-7 | SUPPLY — CI pip-audit (backend) + `npm audit --audit-level=high` (frontend): non-blocking report first, blocking on criticals after a baseline triage recorded in PROGRESS.md; pin the API image base digest + document the rebuild cadence; Renovate/Dependabot optional (owner choice) | CI config, Dockerfile, docs | audits run in CI; a seeded known-vuln in a test manifest is caught | — | **done** — `test_pr7_supply_chain.py` (3): new CI job `supply-chain` runs pip-audit (backend, full report non-blocking + a CRITICAL-only blocking gate) + `npm audit --audit-level=high` (frontend); the API image base is **pinned by digest** (`python:3.11-slim@sha256:e031…d8b3`, resolved from the real registry) with the rebuild cadence documented at the pin; the SCAN commit's pinned scanner versions (checkov 3.3.8 / tfsec v1.28.14) are kept. A seeded known-vuln caught in CI = DLV-37 (deferred to a live CI run). Live = DLV-37 |
| BUGFIX-1 | gcp.cloudsql engine input un-normalized (live acceptance run 2: card showed `Approved engine (PostgreSQL) = false, detail='postgres'` — the extractor's own word went straight through; TF would also have rejected the enum at apply) | `schemas/workflows.py` `GCPCloudSQLInputs._normalize_engine` | the exact live shape passes: `database_version="postgres"` → `POSTGRES_15` → engine check green; unmappable engine → honest ValueError with examples; suite + canary green | — | **done** — `test_bugfix1_cloudsql_engine.py` (11): live pin (bare `postgres` → `POSTGRES_15`, check True, detail canonical); honest spellings canonicalize (`PostgreSQL`/`postgresql 16`/`postgres-14`/`mysql`/`sqlserver 2019 standard`); B1 canonical passthrough + B2 default unchanged; a VALID non-postgres engine still fails the approved-engine check VISIBLY (normalization never hides org policy); junk (`oracle`) rejected with examples, never guessed; **the live card's other false check (`No world-open authorized networks`, detail `0.0.0.0/0`) pinned as MS-9/B2 BY-DESIGN** (legacy default fails visibly to the approver; scoped CIDRs pass; private_network switches the check) — bug #3's scope corrected accordingly |


| BUGFIX-2 | DEP ask never converges across turns (live acceptance run 2: the parent-VPC ask «Which vpc…? Or say "new" to create one» repeated IDENTICALLY on every reply — "new" and "accept-web3-net" were re-resolved from scratch; `_WANTS_NEW` only matches the in-sentence "new vpc" form and nothing mapped the reply onto the slot) | `agents/dependency.py` (`Closure.parent_type`, `choice_from_reply`, `resolve_closure(dep_choice=)`), `agents/cloudops.py` (persist `dep_ask` beside the pending params record; map the next turn's reply before re-resolving) | the exact live utterance sequence converges: opener → params → ask → "new" → create-first DAG (ONE approval); a candidate's name narrows to that parent's REAL recorded facts; an unrecognized reply re-asks honestly; router continuation keeps treating the replies as answers; suite + canary green | — | **done** — `test_bugfix2_dep_convergence.py` (12): the mapper on the exact live ask payload ("new"→`__new__`, "accept-web3-net"→that candidate, "New."/"a new one"/provider-id variants, unrecognized→None never guessed); the resolver honors the choice on exactly the asked slot (`__new__`→DAG `[aws.vpc, aws.ec2]` with the parent named after the child; named candidate→complete with `subnet_id` from its recorded `public_subnet_ids[0]` + provenance note; a candidate WITHOUT recorded facts→asks again honestly; a different slot's choice changes nothing); the OLD bug shape pinned (bare "new" with no dep_choice still asks); the ask now carries `parent_type`+options (what the next turn needs); single-shot "inside a new vpc" still goes straight to DAG (how DLV-12 passed); router pins (`is_new_request` False for "new"/name/id replies, the opener stays "request"). **LIVE-VERIFIED (creds-6)**: the exact failing sequence now lands at the one-approval DAG card (`accept-ec2b-net`→`accept-ec2b`, add=23, awaiting_approval; run then cancelled, nothing applied) |

| BUGFIX-3 | COMP false-positive on database noun phrases (live acceptance run 2: "create a postgres cloudsql instance named accept-sql" was compound-intercepted as "the VM, the database" — `_detected_categories` counted the word "instance" as compute inside ONE resource's own name; Cloud SQL/RDS products are literally named "instances") | `agents/cloudops.py` (`_DB_QUALIFIED` collapse before category detection) | the exact live utterance falls through as ONE resource; genuine compounds (free-standing instance, db AND another resource) stay caught; utterance matrix extended | — | **done** — `test_bugfix3_comp_detection.py` (5) + `test_comp_honest_handling.py` matrix extended: the live utterance is not compound; "rds instance"/"mysql server"/"sql server instance"/"cloud sql instance" are single database resources; a free-standing "instance and a bucket" is still compute+storage compound; "rds instance and an s3 bucket" and "a vm and a postgres database" stay compound; "ec2 instance" keeps meaning compute |

**Behavior bugfixes from live acceptance (owner-ordered, NOT part of CLN-1):** BUGFIX-1 (above, done) → BUGFIX-2 (above, done) → BUGFIX-3 (above, done) → inventory honesty (mark rotated-away sandbox orphan rows unreachable/expired, logged + reversible) → CLN-1 (removal-only) → Phase-3 exit report.

**Phase 3 exit gate:** VPC→EC2 DAG demo e2e in UI · drift notification from manual console change · world-model destroy warning · module proposal→review→promotion flow · **PR-1..3 demonstrated · PR-5 restore drill executed · PR-6 alert rules shipped** (per `docs/prod/PROD_READY_PACK.md`). MODSEED carries its own two STOPs (evidence table after MS-6, again after MS-13) ahead of this gate. — **status: pending**
